[
  {
    "id": "task-a16e36ea",
    "title": "feat: chalk archive — compact released tasks + old events into .chalk/archive",
    "state": "done",
    "acceptanceCriteria": [
      {
        "text": "chalk archive moves done+released tasks (stamped archivedAt) to .chalk/archive/tasks-<year>.json and only their event lines to updates-<year>.jsonl; global events and other tasks' events stay live"
      },
      {
        "text": "a candidate referenced by a remaining task's after is KEPT with a printed reason; done-unreleased tasks stay (release idempotency); archived tasks are never re-released"
      },
      {
        "text": "true"
      },
      {
        "text": "the portal still shows archived delivered scope (portalModel reads the archive); backlog stays coherent after compaction"
      }
    ],
    "tests": [
      {
        "path": "test/archive.test.mjs",
        "sha256": "15caf94f23e49f9e99441d05b5361a4c7a5a2c52848d77ee895f53e6cc5318ef"
      }
    ],
    "heldOut": [],
    "milestone": "oss-launch",
    "after": [],
    "createdAt": "2026-07-02T05:01:01.565Z",
    "reviews": [
      {
        "at": "2026-07-02T06:21:20.944Z",
        "by": "adversary",
        "verdict": "pass",
        "findings": [
          {
            "severity": "low",
            "area": "test-adequacy",
            "note": "Criterion 4 'backlog stays coherent after compaction' is only asserted by `chalk backlog` exiting 0. The dangling-after-edge risk is avoided by design (dep-referenced candidates are KEPT, and B is kept in the fixture), so no archived task can be a live after-target — but there is no direct negative test that a dependency edge into the archive is impossible; coherence rests on the KEEP rule rather than an explicit assertion."
          },
          {
            "severity": "low",
            "area": "test-adequacy",
            "note": "The printed KEEP 'reason' text (`dep-referenced by ...`) is not asserted verbatim — only `/kept B ships/` is matched. The presence of a human reason string is thus not strictly pinned, though the criterion ('KEPT with a printed reason') is substantively covered."
          },
          {
            "severity": "low",
            "area": "correctness",
            "note": "The post-archive `emitUpdate` progress-update and the exact dry-run event count (countEvents) are untested cosmetics; harmless. archivedTasks correctly returns [] when the archive dir is absent, so portalModel's new merge introduces no regression for non-archived projects."
          }
        ]
      }
    ],
    "startedAt": "2026-07-02T06:19:14.276Z",
    "pipeline": {
      "stage": "reviewed",
      "at": "2026-07-02T06:21:20.944Z"
    },
    "doneAt": "2026-07-02T06:22:46.913Z",
    "released": "0.1.0"
  },
  {
    "id": "task-f6bb962a",
    "title": "chore: cut v0.1.0 via chalk release, seed v0.2 roadmap issues, enable GitHub Discussions",
    "state": "done",
    "acceptanceCriteria": [
      {
        "text": "chalk-protocol@0.1.0 is live on the npm registry and npx chalk-protocol demo completes green from the registry on a clean directory"
      },
      {
        "text": "v0.2 roadmap issues seeded and labeled (#78-#86 + #88 evidence-push + #89 retro-filed); GitHub Discussions enabled"
      },
      {
        "text": "v0.1.0 git tag pushed with a GitHub release; post-release chalk archive compacted the spine (48 tasks, 190KB→4.3KB tasks.json)"
      }
    ],
    "tests": [],
    "heldOut": [],
    "milestone": "oss-launch",
    "after": [
      "task-a16e36ea"
    ],
    "createdAt": "2026-07-02T05:01:09.230Z",
    "reviews": [
      {
        "at": "2026-07-02T10:42:39.380Z",
        "by": "adversary",
        "verdict": "block",
        "findings": [
          {
            "severity": "high",
            "area": "correctness",
            "note": "Criterion 1 (chalk-protocol@0.1.0 live on npm + `npx chalk-protocol demo` green from registry on a clean dir) is entirely external and has zero in-repo evidence. The diff only flips the task from blocked→in-progress and logs a self-authored 'Released v0.1.0' decision in updates.jsonl — a claim, not proof. No registry check, no tarball, no publish workflow run output is present."
          },
          {
            "severity": "high",
            "area": "correctness",
            "note": "Criterion 2 (v0.2 roadmap issues #78-#86, #88, #89 seeded/labeled; GitHub Discussions enabled) is wholly unverifiable from this change. Nothing in the diff or working tree references these issues, labels, or Discussions state. The change provides no artifact demonstrating the issues exist or Discussions were turned on."
          },
          {
            "severity": "high",
            "area": "test-adequacy",
            "note": "The task ships real behavior (a release + external roadmap/repo config) with NO locked tests and NO asserting artifact. Every criterion could be false while this diff still applies cleanly — it is a vacuous, self-declared completion. Per the hard rule, behavior added with no test asserting it must block."
          },
          {
            "severity": "med",
            "area": "correctness",
            "note": "Criterion 3 is only partially evidenced: the archive did compact the spine (48 tasks moved to .chalk/archive/tasks-2026.json, 3 tasks remain live), which is checkable. But the git tag v0.1.0 push and the GitHub release cannot be confirmed from the diff — `git tag`/release state is not shown, and the exact '190KB→4.3KB' figure is unverified."
          },
          {
            "severity": "low",
            "area": "design-intent",
            "note": "The diff records the task as merely 'in-progress' with pending todos, inconsistent with a change presented as having completed the v0.1.0 cut; the metadata state does not itself substantiate that the release chore is finished."
          }
        ]
      }
    ],
    "handoff": {
      "path": ".chalk/handoffs/task-f6bb962-1.md",
      "at": "2026-07-02T06:39:58.781Z",
      "reason": "human-input",
      "seq": 1
    },
    "startedAt": "2026-07-02T10:41:57.165Z",
    "doneAt": "2026-07-02T10:44:01.472Z"
  },
  {
    "id": "task-021f4985",
    "title": "fix: chalk release --commit — commit CHANGELOG+version bump, then tag that commit (removes the release.yml tag-normalization step)",
    "state": "done",
    "acceptanceCriteria": [
      {
        "text": "chalk release --commit writes CHANGELOG.md + the package.json bump, creates a git commit containing exactly those release artifacts (conventional message chore(release): vX.Y.Z), then tags vX.Y.Z on that commit — git show vX.Y.Z:package.json reports the bumped version"
      },
      {
        "text": "tag-collision safety is preserved under --commit: when tag vX.Y.Z already exists the command dies BEFORE writing CHANGELOG, bumping package.json, committing, or marking any task released"
      },
      {
        "text": "without --commit the existing tag-first behavior is unchanged (all existing release tests stay green)"
      },
      {
        "text": "release.yml drops the 'npm pkg set version' tag-normalization step (the tagged tree is published as-is) and its header comment reflects the commit-then-tag flow"
      },
      {
        "text": "chalk help documents the --commit flag on the release line"
      }
    ],
    "tests": [
      {
        "path": "test/package.test.mjs",
        "sha256": "e612e65441724f18d3f457079a3a0766148ffe583dc7254e4f9412ca05a43554"
      },
      {
        "path": "test/release-commit.test.mjs",
        "sha256": "526ff248659bb61969c0568ac8f91db947243251d71c540311440f6deff162e3"
      }
    ],
    "heldOut": [],
    "milestone": "v0.2",
    "after": [],
    "createdAt": "2026-07-02T06:34:07.475Z",
    "reviews": [
      {
        "at": "2026-07-06T06:53:57.277Z",
        "by": "adversary",
        "verdict": "pass",
        "findings": [
          {
            "severity": "low",
            "area": "test-adequacy",
            "note": "test/package.test.mjs was legitimately amended (amend-spec logged in .chalk/decisions.md) and its assertion now requires the ABSENCE of 'npm pkg set version', but the file's header comment (lines 6–7) still says the workflow 'normalizes the version from the tag (chalk tags before the bump lands)' — stale prose contradicting the flipped assertion; should have been cleaned in the same amendment."
          },
          {
            "severity": "low",
            "area": "correctness",
            "note": "Partial-failure edge under --commit: the collision is probed up front (gitTry rev-parse), but if `git tag` fails AFTER the release commit for a non-collision reason (hook, perms, or a tag created in the probe→tag window), the die() leaves an untagged chore(release) commit at HEAD with the bumped package.json; a re-run then computes the NEXT version from the already-bumped version (version skip). Criterion 2 (pre-existing tag dies before writing) is still satisfied and tested, but this recovery path is untested and slightly worse than tag-first."
          },
          {
            "severity": "low",
            "area": "regression",
            "note": "test/release-commit.test.mjs — the locked contract for this task — does not appear in the git diff --stat (8 files, none of them this file), implying it is untracked-new in the working tree. It exists and its sha256 is pinned in .chalk/tasks.json, but make sure it is committed with the change; otherwise CI (node --test in release.yml) and any fresh checkout would run without the very tests that pin --commit behavior."
          },
          {
            "severity": "low",
            "area": "design-intent",
            "note": "--commit --no-tag skips the up-front collision probe entirely (wantTag=false), committing the bump with no tag check — consistent with --no-tag semantics and the documented escape hatch, but the combination is untested; noting for completeness, not a blocker."
          }
        ]
      },
      {
        "at": "2026-07-06T06:54:27.221Z",
        "by": "amend-spec",
        "verdict": "stale",
        "note": "locked test amended after review — re-review required"
      },
      {
        "at": "2026-07-06T06:56:41.377Z",
        "by": "adversary",
        "verdict": "pass",
        "findings": [
          {
            "severity": "med",
            "area": "regression",
            "note": "test/release-commit.test.mjs — the locked contract pinning every --commit behavior — does not appear in git diff --stat (8 changed files, none of them this one), meaning it is an untracked NEW file in the working tree. Its sha256 is pinned in .chalk/tasks.json and it exists on disk, but if it is not committed with the change, CI (node --test in release.yml) and any fresh checkout run WITHOUT the tests that guard criteria 1–3 and 5 — a vacuous green. Ensure `git add test/release-commit.test.mjs` lands in the same commit before `chalk done`."
          },
          {
            "severity": "low",
            "area": "correctness",
            "note": "Partial-failure path under --commit: the collision is probed up front (gitTry rev-parse refs/tags/vX.Y.Z), but if `git tag` fails AFTER the release commit for a non-collision reason (hook, perms, probe→tag race), die() leaves an untagged chore(release) commit at HEAD with the bumped package.json; a re-run then bumps FROM the already-bumped version (version skip) and would produce a second release commit. Criterion 2 (pre-existing tag) is satisfied and tested; this adjacent recovery path is untested and slightly worse than tag-first."
          },
          {
            "severity": "low",
            "area": "test-adequacy",
            "note": "--commit --no-tag skips the up-front collision probe entirely (wantTag=false), committing the bump with no tag check; the combination is consistent with --no-tag semantics but exercised by no test."
          },
          {
            "severity": "low",
            "area": "test-adequacy",
            "note": "Criterion 4's second clause ('header comment reflects the commit-then-tag flow') is prose-only: the locked test asserts the absence of `npm pkg set version` but nothing about the comment. Verified manually that release.yml lines 5–7 now describe commit-then-tag; acceptable since comments are not machine-testable behavior."
          },
          {
            "severity": "low",
            "area": "design-intent",
            "note": "test/package.test.mjs (locked) was modified, but via two sanctioned amend-spec entries logged in .chalk/decisions.md (flip the workflow assertion to forbid the normalization step; sweep the stale header prose the prior review flagged), the sha256 is re-pinned in tasks.json, and this re-review covers the post-amendment state — the legitimate path was followed."
          }
        ]
      }
    ],
    "startedAt": "2026-07-06T06:46:13.619Z",
    "pipeline": {
      "stage": "cleaned",
      "at": "2026-07-06T07:46:35.875Z"
    },
    "branch": "fix/release-commit-tags-bumped-commit",
    "branchType": "fix",
    "pr": {
      "number": 93,
      "url": "https://github.com/chalkagents/chalk-protocol/pull/93",
      "recorded": true,
      "lgtm": true
    },
    "doneAt": "2026-07-06T07:46:35.874Z"
  },
  {
    "id": "task-11b3c047",
    "title": "fix: CONFIG.md drift gate only validates top-level protocol keys",
    "state": "done",
    "acceptanceCriteria": [
      {
        "text": "Recurse into nested protocol.* keys (dotted paths) when comparing documented vs initialized config"
      },
      {
        "text": "Add a test asserting a documented-but-removed nested key (and an undocumented new nested key) fails the gate"
      }
    ],
    "tests": [
      {
        "path": "test/docs.test.mjs",
        "sha256": "a4874a65580e75221f623b76c576aacd562bdd18dfd3b2ad663ce4e5ce55191e"
      },
      {
        "path": "test/config-drift.test.mjs",
        "sha256": "60e9617ee7edf3c94188f759e54717e42f1a449d54cb3945bf9d47acdc1ff58e"
      }
    ],
    "heldOut": [],
    "after": [],
    "issue": {
      "number": 89,
      "url": "https://github.com/chalkagents/chalk-protocol/issues/89",
      "body": "test/docs.test.mjs compares docs/CONFIG.md against `Object.keys(meta.protocol)` — only top-level keys. Nested keys (verify.test, review.requiredAt, github.mergeMethod, handoff.maxAttempts, etc.) can drift from the docs or be added to initSpine without the gate noticing, so the intended 'every protocol.* key is documented' guarantee holds only at the top level.\n\n- [ ] Recurse into nested protocol.* keys (dotted paths) when comparing documented vs initialized config\n- [ ] Add a test asserting a documented-but-removed nested key (and an undocumented new nested key) fails the gate\n\n_filed by `chalk retro` (self-healing)_"
    },
    "branchType": "fix",
    "labels": [
      "enhancement"
    ],
    "pipeline": {
      "stage": "cleaned",
      "at": "2026-07-06T07:47:03.937Z"
    },
    "createdAt": "2026-07-06T06:46:13.554Z",
    "reviews": [
      {
        "at": "2026-07-06T07:06:07.369Z",
        "by": "adversary",
        "verdict": "pass",
        "findings": [
          {
            "severity": "med",
            "area": "correctness",
            "note": "configDrift checks exactly ONE nested level, not true recursion: level-3 keys (e.g. protocol.github.labelType.bug) are explicitly out of scope. Criterion 1 says 'recurse into nested protocol.* keys'. Mitigated: every schema-bearing key in initSpine is at depth ≤2 (deeper levels are data maps / gate value shapes), the scope choice is documented in the code comment, logged via the sanctioned amend-spec decision, and locked by a dedicated test ('recursion stops one level deep'), so the shipped behavior matches the locked contract."
          },
          {
            "severity": "med",
            "area": "correctness",
            "note": "False-negative edge: `if (!isPlainObj(val) || !Object.keys(val).length) continue;` means if a protocol key's default becomes an EMPTY object (all nested keys removed) or degrades to a scalar/array while the doc still lists a `{ … }` key list, the documented-but-removed nested keys are NOT flagged. Criterion 2's specific single-key-removal scenario is covered, but the all-keys-removed case silently passes and no test pins it."
          },
          {
            "severity": "low",
            "area": "correctness",
            "note": "The `{ … }` key list is taken from the FIRST inline-code brace group in a section (`/`\\{([^}`]*)\\}/`). If future prose adds an illustrative `{ … }` snippet before the real key list (as `verify` already does after its list), the gate compares against the wrong group. Duplicate `### `key`` headings also silently overwrite each other in the sections Map."
          },
          {
            "severity": "low",
            "area": "regression",
            "note": "lib/store.mjs default-config changes (review.required→requiredAt:[], regression.locPerTest:2000, github.ciPollIntervalMs/ciPollAttempts) are scope creep for a docs-drift task, but they are forced by the new gate (docs already documented those keys), behavior-neutral (reviewCadences, heldOutFloor, and brokecheck all carry identical fallbacks), and covered — reverting store.mjs alone now fails the real-config drift tests."
          },
          {
            "severity": "low",
            "area": "test-adequacy",
            "note": "Test adequacy is genuinely strong: test/config-drift.test.mjs asserts both required negative cases (undocumented NEW nested key, documented-but-REMOVED nested key) with exact problem counts and dotted-path messages, plus bidirectional top-level drift, missing-key-list failure, and a non-vacuity guard (verify must still carry ≥4 nested keys). Reverting to the old top-level-only gate would fail these tests, so the change is non-vacuously covered. Locked test/docs.test.mjs was modified, but via the sanctioned amend-spec path (decision logged, sha256 re-pinned in tasks.json)."
          }
        ]
      },
      {
        "at": "2026-07-06T07:07:54.053Z",
        "by": "amend-spec",
        "verdict": "stale",
        "note": "locked test amended after review — re-review required"
      },
      {
        "at": "2026-07-06T07:09:24.582Z",
        "by": "adversary",
        "verdict": "pass",
        "findings": [
          {
            "severity": "med",
            "area": "correctness",
            "note": "Criterion 1 says 'recurse' but configDrift() checks exactly ONE nested level: depth-3 keys (e.g. protocol.github.labelType.bug) are explicitly out of scope. Mitigated: CONFIG.md's format can only express one level of schema (a `{ … }` key list per section), every schema-bearing initSpine key is at depth ≤2 (deeper levels are data maps / value shapes), the scope is documented in the code comment, logged via the sanctioned amend-spec decisions, and pinned by a dedicated locked test ('recursion stops one level deep'). The shipped behavior satisfies the issue's intent (nested keys can no longer drift silently) and both required negative cases emit dotted-path problems (protocol.verify.build, protocol.handoff.maxAttempts)."
          },
          {
            "severity": "low",
            "area": "test-adequacy",
            "note": "Criterion 2 is directly and non-vacuously covered: test/config-drift.test.mjs asserts the undocumented-NEW-nested-key and documented-but-REMOVED-nested-key failures with exact problem counts and dotted-path regexes, plus the degraded-to-scalar/empty-object edge (both '' and {}), bidirectional top-level drift, the missing-`{ … }`-list failure, the one-level scope, and a real-config non-vacuity guard (verify must carry ≥4 nested keys). Reverting configDrift or the store.mjs default additions alone fails these tests. Note: test/config-drift.test.mjs does not appear in the diff stat — it is presumably a new untracked file; confirm it is committed alongside the change, or the locked pin in tasks.json points at a file that never ships."
          },
          {
            "severity": "low",
            "area": "correctness",
            "note": "The `{ … }` key list is the FIRST `` `{ `` group in a section (/`\\{([^}`]*)\\}/). If future prose inserts an illustrative brace snippet before the real key list (the review section already carries `{\"verdict\":…}` later in its body), the gate compares the wrong group — this fails LOUD (false positive), not silent, so drift cannot slip through, but it is a maintenance trap. Duplicate `### `key`` headings also silently overwrite each other in the sections Map."
          },
          {
            "severity": "low",
            "area": "regression",
            "note": "lib/store.mjs default changes (review.required:false → requiredAt:[], regression.locPerTest:2000, github.ciPollIntervalMs:5000/ciPollAttempts:24) are scope creep for a docs-drift task, but they are forced by the now-honest gate (docs documented those keys), behavior-neutral for new inits (reviewCadences honors legacy required, heldOutFloor defaults to 2000, brokecheck falls back to 5000/24), and cannot touch existing configs (initSpine only runs at init). The locked test/docs.test.mjs was modified, but via the sanctioned amend-spec path — two decisions logged, sha256 re-pinned, prior review correctly marked stale triggering this re-review."
          }
        ]
      }
    ],
    "startedAt": "2026-07-06T07:09:53.878Z",
    "branch": "fix/89-config-drift-nested-keys",
    "pr": {
      "number": 94,
      "url": "https://github.com/chalkagents/chalk-protocol/pull/94",
      "recorded": true,
      "lgtm": true
    },
    "doneAt": "2026-07-06T07:47:03.936Z"
  },
  {
    "id": "task-459bc189",
    "title": "fix: evidence-push failures are swallowed (catch{}) — blob-SHA 404s surface as broken PR images",
    "state": "done",
    "acceptanceCriteria": [
      {
        "text": "a failed git push in chalk evidence is no longer swallowed: a visible ⚠ warning line names the push failure and its cause"
      },
      {
        "text": "when the push fails, the PR body is NOT edited with blob-SHA image URLs (no 404 images land in the PR); the final status line says the screenshots were captured but not attached"
      },
      {
        "text": "when the push succeeds, behavior is unchanged: the evidence markdown with commit-SHA blob URLs is appended to the PR body"
      },
      {
        "text": "an e2e test drives the REAL push path via the bare-remote + stub-gh fixture pattern (per repoWithBare in test/gate-hardening.test.mjs): one case with a working remote asserting the body edit, one with a broken remote asserting the ⚠ line and the skipped body edit"
      }
    ],
    "tests": [
      {
        "path": "test/evidence-push.test.mjs",
        "sha256": "ea372902aa3ffa7008d446d2e7bec481d94655f76147164a5974db196a00edda"
      }
    ],
    "heldOut": [],
    "after": [],
    "issue": {
      "number": 88,
      "url": "https://github.com/chalkagents/chalk-protocol/issues/88",
      "body": "**From the harness review (finding 7, the last unswept silent failure).** `chalk evidence` attaches screenshots to the PR via blob-SHA URLs; the push path swallows errors (`catch {}`), so a failed push yields 404 image links in the PR body with no warning at the source.\n\n**Why it wasn't fixed in the gate-hardening PR (#70):** pinning it honestly needs an e2e/gh worktree fixture (a bare-remote + stub-gh scaffold that exercises the real push path) — heavier than the one-line warn, and the wave's break-it rule forbids shipping the warn untested.\n\n**Sketch:** reuse the `repoWithBare()` + stub-gh pattern from test/gate-hardening.test.mjs to drive `chalk evidence` against a push that fails, assert a ⚠ line and that the PR body skips (not 404s) the missing asset."
    },
    "branchType": "fix",
    "labels": [
      "enhancement"
    ],
    "pipeline": {
      "stage": "cleaned",
      "at": "2026-07-06T07:47:28.787Z"
    },
    "createdAt": "2026-07-06T06:46:13.557Z",
    "reviews": [
      {
        "at": "2026-07-06T07:17:32.251Z",
        "by": "adversary",
        "verdict": "block",
        "findings": [
          {
            "severity": "high",
            "area": "correctness",
            "note": "Criterion 1 requires the ⚠ line to name the push failure AND its cause. The code prints `String(e.message || e).split('\\n')[0]`, but for Node's execSync (lib/git.mjs git()) the error message is `Command failed: git push\\n<stderr>` — the actual cause (git's stderr, e.g. 'does not appear to be a git repository', auth/network errors) is on subsequent lines and is discarded. The warning therefore only ever says 'Command failed: git push', which names the failure but not its cause. Use the later lines of e.message (or e.stderr) instead of line 0."
          },
          {
            "severity": "med",
            "area": "test-adequacy",
            "note": "The locked test only asserts /⚠.*push failed/i for the failure case; it never asserts that the underlying cause (git stderr text) appears in the warning, so the cause-dropping bug above passes GREEN. A vacuous match on the fixed prefix cannot distinguish 'names the cause' from 'names only the command'."
          },
          {
            "severity": "low",
            "area": "design-intent",
            "note": "On push failure the task still advances to stage 'tested' and t.evidence is recorded, and the emitted update says 'Evidence attached (N screenshot(s))' even though nothing was attached to the PR. The stage advance is locked by the test (re-run idempotency), but a later remote fix leaves the screenshots permanently unattached ('evidence (already done)'), and the update-feed title is misleading."
          },
          {
            "severity": "low",
            "area": "regression",
            "note": "Happy path is structurally unchanged (same push→rev-parse→pr view→pr edit sequence gated behind pushed=true) and covered by the working-remote e2e case; no regression identified there. Note the locked test file itself does not appear in the diff/stat — it exists in the working tree but appears untracked; ensure it is committed with the change so the locked contract actually ships."
          }
        ]
      },
      {
        "at": "2026-07-06T07:20:32.323Z",
        "by": "adversary",
        "verdict": "pass",
        "findings": [
          {
            "severity": "low",
            "area": "test-adequacy",
            "note": "The prior block is resolved: runGit uses execSync with stdio 'pipe' + utf8, so e.stderr carries git's stderr; the cause extractor skips the 'Command failed: git push' line and the locked test now asserts the real git error text (/does not appear to be a git repository|Could not read from remote/), so the warning cannot regress to a vacuous prefix. Both criteria-4 cases drive the REAL push path (bare remote happy path asserting the pr-edit body with commit-SHA blob URLs; broken remote asserting the ⚠ cause line, the /NOT attached/ status line, the skipped pr edit via gh.log, and the local evidence commit). Reverting the fix would fail the failure-case test, so the coverage is non-vacuous."
          },
          {
            "severity": "low",
            "area": "regression",
            "note": "test/evidence-push.test.mjs exists in the working tree and its sha256 is locked in .chalk/tasks.json, but it does not appear in the git diff/stat — it looks untracked. Ensure the locked test is committed alongside bin/chalk.mjs before merge, or the shipped change lands without its contract test in history."
          },
          {
            "severity": "low",
            "area": "design-intent",
            "note": "On push failure the stage still advances to 'tested' and t.evidence is recorded, so a later remote fix leaves screenshots permanently unattached ('evidence (already done)'). This is now an explicit, locked design choice (the test asserts stage==='tested' for re-run idempotency) and the update-feed title honestly says 'push failed, not attached', so it is accepted — but a future 'evidence --retry-attach' path may be worth a follow-up task."
          }
        ]
      }
    ],
    "startedAt": "2026-07-06T07:12:50.124Z",
    "branch": "fix/88-evidence-push-honesty",
    "pr": {
      "number": 95,
      "url": "https://github.com/chalkagents/chalk-protocol/pull/95",
      "recorded": true,
      "lgtm": true
    },
    "doneAt": "2026-07-06T07:47:28.787Z"
  },
  {
    "id": "task-9bfdd13a",
    "title": "fix: sameModelFamily can't see env-var models (CHALK_OPENCODE_MODEL) — cross-model warning inert for opencode",
    "state": "done",
    "acceptanceCriteria": [
      {
        "text": "sameModelFamily/modelSignature see the model of opencode-adapter commands whose model comes from the CHALK_OPENCODE_MODEL env var, so the doctor's same-model-reviewer warning can fire for opencode users"
      },
      {
        "text": "the fallback stays conservative: no env var and no --model flag means no model identity (no false alarms), and a non-opencode command is unaffected"
      },
      {
        "text": "a test covers: two opencode-adapter commands under the same CHALK_OPENCODE_MODEL → same family (warning fires); differing explicit --model still wins over the env var"
      }
    ],
    "tests": [
      {
        "path": "test/crossmodel-env.test.mjs",
        "sha256": "508f1c3685d4cfffec978cf509a88825d3a4c94bae25ca05c6329b30e7fd17c9"
      }
    ],
    "heldOut": [],
    "after": [],
    "issue": {
      "number": 85,
      "url": "https://github.com/chalkagents/chalk-protocol/issues/85",
      "body": "**Known nit from the opencode integration (#65 review).** `modelSignature()` parses only the command string (binary + --model flag). opencode users set the model via `CHALK_OPENCODE_MODEL`, so the doctor's same-model-reviewer warning (self-preference bias) can never fire for them.\n\n**Sketch:** teach `modelSignature` an env fallback per adapter (e.g. read `CHALK_OPENCODE_MODEL` when the command resolves to the opencode adapters), or let adapters declare their model via a comment/flag; keep it conservative (no false alarms)."
    },
    "branchType": "fix",
    "labels": [
      "good first issue"
    ],
    "pipeline": {
      "stage": "cleaned",
      "at": "2026-07-06T07:47:43.582Z"
    },
    "createdAt": "2026-07-06T06:46:13.558Z",
    "reviews": [
      {
        "at": "2026-07-06T07:30:14.902Z",
        "by": "adversary",
        "verdict": "pass",
        "findings": [
          {
            "severity": "low",
            "area": "correctness",
            "note": "Adapter detection requires the script path at toks[1] (`node <path>`); a command like `node --experimental-x …/opencode-exec.mjs` or a quoted path with spaces escapes detection. Consistent with the documented adapter invocation and pre-existing tokenizer limits, but worth knowing."
          },
          {
            "severity": "low",
            "area": "correctness",
            "note": "The regex `/opencode-(exec|json)\\.mjs$/` is only end-anchored, so a user script named e.g. `my-opencode-exec.mjs` would be misclassified as the bundled adapter and inherit the env-var model. Unlikely in practice; would only produce a warning, not a gate failure."
          },
          {
            "severity": "low",
            "area": "correctness",
            "note": "Two opencode adapters with NO env var and no --model still compare equal (bin 'opencode', model '' on both) and flag as same family. This mirrors the existing `claude -p` vs `claude -p` semantics (same binary + absent model is flagged) and is arguably correct — both would use opencode's default model — but no test pins sameModelFamily(EXEC, JSON_, {}) either way, so the intended semantics there are undocumented."
          },
          {
            "severity": "low",
            "area": "test-adequacy",
            "note": "Coverage is genuinely adequate: reverting the lib/config.mjs change fails the locked unit tests (env-var resolution, --model precedence, non-opencode conservatism) AND the e2e doctor test proves the warning fires through the real process.env path via lib/doctor.mjs line 68. The env param is optional with a process.env default, so existing callers (doctor) and the prior locked crossmodel.test.mjs are unaffected — no regression found."
          }
        ]
      }
    ],
    "startedAt": "2026-07-06T07:27:16.546Z",
    "branch": "fix/85-crossmodel-env-model",
    "pr": {
      "number": 96,
      "url": "https://github.com/chalkagents/chalk-protocol/pull/96",
      "recorded": true,
      "lgtm": true
    },
    "doneAt": "2026-07-06T07:47:43.581Z"
  },
  {
    "id": "task-2563a7a0",
    "title": "feat: chalk release --promote — protected-main release flow (promotion PR + tag on main's tip)",
    "state": "done",
    "acceptanceCriteria": [
      {
        "text": "run the `--commit --no-tag` flow on the integration branch (github.base)"
      },
      {
        "text": "open the promotion PR to the deploy branch (new config, e.g. `github.deployBase` or `release.promoteTo`), wait for CI, merge with a MERGE commit regardless of github.mergeMethod"
      },
      {
        "text": "tag the deploy branch's tip vX.Y.Z and push the tag"
      },
      {
        "text": "collision/idempotency safety consistent with #93 and #91 (a failed step must not mark tasks released or strand a half-promoted release)"
      }
    ],
    "tests": [
      {
        "path": "test/release-promote.test.mjs",
        "sha256": "16fb0eb3a0ff41e12f0dd9e72053c4d41c2bf4856196dcf2203a16ffa5f1cb0c"
      }
    ],
    "heldOut": [],
    "after": [],
    "issue": {
      "number": 98,
      "url": "https://github.com/chalkagents/chalk-protocol/issues/98",
      "body": "**Context:** the repo moved to a dev/main model (2026-07-06): all PRs target `dev` (protocol.github.base=dev), `main` is deployable and protected (PR required, `test` check, enforce_admins, no direct pushes). That protection rejects the push `chalk release --commit` (#93) makes to the branch it runs on.\n\n**Working manual flow today:**\n1. `chalk release --commit --no-tag` on **dev** (CHANGELOG + bump land as a normal commit)\n2. promotion PR dev→main, merged with a **merge commit** (not squash — preserves the release commit SHA)\n3. tag `vX.Y.Z` on main's tip and push the tag (tag pushes bypass branch protection; release.yml publishes the tagged tree as-is)\n\n**Ask:** teach `chalk release` this path natively, e.g. `chalk release --promote`:\n- [ ] run the `--commit --no-tag` flow on the integration branch (github.base)\n- [ ] open the promotion PR to the deploy branch (new config, e.g. `github.deployBase` or `release.promoteTo`), wait for CI, merge with a MERGE commit regardless of github.mergeMethod\n- [ ] tag the deploy branch's tip vX.Y.Z and push the tag\n- [ ] collision/idempotency safety consistent with #93 and #91 (a failed step must not mark tasks released or strand a half-promoted release)\n\n_filed while setting up the dev/main branch model_"
    },
    "branchType": "feat",
    "labels": [
      "enhancement"
    ],
    "pipeline": {
      "stage": "cleaned",
      "at": "2026-07-06T08:52:51.402Z"
    },
    "createdAt": "2026-07-06T08:12:17.231Z",
    "reviews": [
      {
        "at": "2026-07-06T08:39:54.276Z",
        "by": "adversary",
        "verdict": "block",
        "findings": [
          {
            "severity": "high",
            "area": "correctness",
            "note": "Criterion 4 is violated in the tag-created-but-push-failed window: if `git tag` (line 616) succeeds but `push origin vX` (line 621) fails, the die says 'Re-run to finish' — but the re-run CANNOT resume. Orphan detection (line 515) requires the tag to be ABSENT locally (`rev-parse refs/tags/vX === ''`), so with the local tag present resume=null, the collision probe is also skipped (wantTag=false under promote), and the run falls into the fresh path: it re-bumps from the already-bumped package.json (0.1.0→0.2.0), writes a second CHANGELOG section for the same tasks, stacks a second `chore(release)` commit, opens a second promotion PR, and never pushes v0.1.0 — the exact #91 version-skip bug reintroduced in the promote flow, plus a stranded half-promoted release. The 'idempotent tagging'/ls-remote skips at lines 615/619 are unreachable in the very scenario they were written for, because reaching them requires resume, and resume requires the tag to not exist."
          },
          {
            "severity": "high",
            "area": "correctness",
            "note": "Post-merge failures (fetch fails at line 613, or tag creation fails at 617) are also un-resumable against real GitHub: the re-run's resume path re-executes the FULL PR choreography — `pr list --state open` finds nothing (the PR is merged), so it runs `pr create dev→main`, which real `gh` rejects with 'no commits between main and dev' once the merge has landed, and the run dies at line 596 before ever reaching the tag step. The die messages promise 'The PR merged; re-run to finish the tagging', but the code never skips ahead to tagging when the deploy branch already contains the release commit. The locked test's gh stub unconditionally succeeds at `pr create`, masking this; the only re-run exercised (test 3) fails PRE-merge at RED CI."
          },
          {
            "severity": "med",
            "area": "correctness",
            "note": "#93's collision safety ('fail before anything is written') is dropped for --promote: because wantTag=false, the up-front probe at line 551 never runs. With a pre-existing stale local tag vX.Y.Z (pointing anywhere), promote writes the release commit, opens/merges the PR (moving protected main), then line 615 silently SKIPS tag creation because the tag exists, and line 619 pushes the stale tag — publishing a vX.Y.Z tag pointing at the wrong commit while marking all tasks released. No collision case is tested for promote."
          },
          {
            "severity": "med",
            "area": "test-adequacy",
            "note": "The idempotency machinery specific to promote is untested: no test injects a failure after `pr merge` (tag-push failure, fetch failure), so the tag-exists skip (615), the ls-remote-guarded push (619), and the merged-fallback `pr view` recheck (607-608) can all be deleted or inverted with the suite staying green. Test 3's re-run only covers the pre-merge RED-CI interruption, which exercises the pre-existing #91 orphan detection, not the promote-specific recovery the criterion demands. The 'CI still pending' abort path is likewise unpinned (only bucket:fail is tested)."
          },
          {
            "severity": "low",
            "area": "test-adequacy",
            "note": "The stub's `pr merge` performs a fast-forward `push dev:main`, so the deploy tip SHA equals dev's HEAD — the tests cannot distinguish 'tag the deploy tip' from 'tag local HEAD'. A real --merge produces a merge commit whose SHA differs from dev HEAD; the implementation does tag FETCH_HEAD correctly, but the locked assertion (remoteMain === dev HEAD === tag target) would also pass an implementation that tags HEAD, weakening criterion 3's pin."
          },
          {
            "severity": "low",
            "area": "design-intent",
            "note": "The diff again bundles unrelated spine changes (new `protocol.worktree` config block in .chalk/chalk.json, imported tasks #92 and the already-done #91 task/board entries) against the repo's 'keep diffs small and scoped' rule — carried-over noise, not a gate by itself."
          }
        ]
      },
      {
        "at": "2026-07-06T08:47:20.567Z",
        "by": "adversary",
        "verdict": "pass",
        "findings": [
          {
            "severity": "med",
            "area": "test-adequacy",
            "note": "The find-or-create PR resolution (line 612 `pr list --head … --state open`) is exercised by test 3's re-run but never ASSERTED: test 3 doesn't count `pr create` lines, and the stub's `pr create` happily succeeds when a PR already exists, so deleting the `pr list` lookup keeps the suite green while real gh would die with 'a pull request already exists' on every pre-merge re-run — an un-resumable pre-merge interruption in production. Add `creates.length === 1` to test 3 (mirroring test 4) next cycle."
          },
          {
            "severity": "med",
            "area": "test-adequacy",
            "note": "The later-arrival cutoff on promote RESUME (lines 644-645: tasks done after the orphan commit are deferred to the next release) is untested in the promote suite — mutating `cutoff` to Infinity on resume stays green, silently absorbing post-interruption tasks into a frozen CHANGELOG. The equivalent behavior is pinned only in release-recovery's non-promote path; the promote path shares the shape but not a test."
          },
          {
            "severity": "low",
            "area": "correctness",
            "note": "The completion marker remains the working-tree `Released vX` decision: on a fresh clone missing .chalk/decisions.md, the first promote after a completed release false-resumes on the old release commit — but the cutoff excludes newer tasks and origin already has the tag, so it degrades to a confusing no-op 'released vOLD' success plus a duplicate decision, then self-heals on the next run. Same accepted residual as the #91 pass review; decisions.md is committed, so the window is narrow."
          },
          {
            "severity": "low",
            "area": "test-adequacy",
            "note": "The `pr view --json state` merged-fallback (lines 628-630) and the ls-remote tag-already-pushed skip (line 638) are individually revertible with the suite green (the stub's merge never fails non-zero-but-merged, and re-pushing an identical tag is a no-op). Both are defensive mirrors of existing `chalk merge` behavior rather than criterion-bearing paths."
          },
          {
            "severity": "low",
            "area": "design-intent",
            "note": "All four previously blocking findings are demonstrably fixed and pinned by the amended locked test: the post-merge tag-push failure now resumes (decision-keyed detection tolerates the leftover local tag, `tag -fa` + ls-remote guard, test 4 asserts ONE `pr create` and 'already merged'), the merged-PR re-run skips the choreography via fetch-first `merge-base --is-ancestor`, the fresh-path collision probe (local+remote, line 561) fails before any write (test 5), pending-CI exhaustion aborts (test 6), and the stub's real commit-tree merge makes 'tag the deploy TIP' distinguishable from 'tag local HEAD' (test 1 asserts remoteMain ≠ dev HEAD and tag→merge-commit). Criterion 2's 'regardless of mergeMethod' is honestly pinned: the config carries the default squash while the log asserts `pr merge 7 --merge`. The lingering diff noise (worktree config block, imported task entries) is carried-over spine state, not code risk."
          }
        ]
      },
      {
        "at": "2026-07-06T08:48:06.644Z",
        "by": "amend-spec",
        "verdict": "stale",
        "note": "locked test amended after review — re-review required"
      },
      {
        "at": "2026-07-06T08:51:05.478Z",
        "by": "adversary",
        "verdict": "pass",
        "findings": [
          {
            "severity": "med",
            "area": "test-adequacy",
            "note": "The remote-tag arm of the fresh-path collision probe (line 561, `ls-remote --tags origin`) is revertible with the suite green: test 5 only plants a LOCAL stale tag. If origin carries vX.Y.Z but the local clone doesn't (fresh clone after a foreign push), deleting that arm lets promote commit + merge, then line 638's ls-remote skip silently treats the stale remote tag as 'already shipped' and marks everything released against a wrong-pointing tag. Add a remote-only-collision case next cycle."
          },
          {
            "severity": "med",
            "area": "test-adequacy",
            "note": "`push origin ${ghBase}` (line 609) is revertible with the suite green: the gh stub's `pr merge` builds the merge commit from the LOCAL dev ref and pushes the objects itself, so the deploy-tip/tag/package.json assertions all pass even if the release commit never reaches origin/dev — against real GitHub the promotion PR would not contain the release commit. An assertion that the bare repo's dev ref equals the local release commit would pin criterion 1's push."
          },
          {
            "severity": "low",
            "area": "test-adequacy",
            "note": "Two defensive paths remain individually revertible: the `pr view --json state` merged-despite-nonzero fallback (lines 628-630; the stub's merge never fails-but-merges) and the ls-remote tag-already-pushed skip (line 638; re-pushing an identical tag is a no-op). Both mirror existing `chalk merge` behavior and are not criterion-bearing — same accepted residual as the prior review."
          },
          {
            "severity": "low",
            "area": "correctness",
            "note": "The completion marker is still the working-tree `Released vX` decision: on a clone missing .chalk/decisions.md the first promote after a completed release false-resumes on the old release commit, degrading to a confusing no-op 'released vOLD' plus a duplicate decision before self-healing. Same narrow accepted residual as #91; decisions.md is committed."
          },
          {
            "severity": "low",
            "area": "design-intent",
            "note": "The diff still bundles unrelated spine noise (the `protocol.worktree` config block in .chalk/chalk.json, imported #92/#91 task and board entries) against the 'keep diffs small and scoped' rule — carried-over state, not code risk."
          },
          {
            "severity": "low",
            "area": "regression",
            "note": "Non-promote paths verified unchanged: `wantTag` only adds `!promote`, and the #91 resume gate `(promote || !tagLocal)` preserves the tag-absent requirement for --commit; release-recovery's locked contract is untouched. Both prior blocking reviews' findings are demonstrably fixed and now pinned by the amended locked test: post-merge tag-push failure resumes past the leftover local tag (test 4, ONE `pr create`, 'already merged'), the merged-PR re-run skips choreography via fetch-first `merge-base --is-ancestor`, the fresh collision probe fails before any write (test 5), pending-CI exhaustion aborts (test 6), the pre-merge re-run FINDS the open PR (test 3 `creates.length===1`), late arrivals are deferred on resume (test 3), and the stub's real commit-tree merge makes 'tag the deploy TIP' distinguishable from 'tag local HEAD' (test 1)."
          }
        ]
      }
    ],
    "startedAt": "2026-07-06T08:31:24.320Z",
    "branch": "feat/98-feat-chalk-release-promote-protected",
    "pr": {
      "number": 103,
      "url": "https://github.com/chalkagents/chalk-protocol/pull/103",
      "recorded": true,
      "lgtm": true
    },
    "doneAt": "2026-07-06T08:52:51.401Z"
  },
  {
    "id": "task-2cae7c80",
    "title": "chore: author the held-out regression set — audit warns 0 tests vs a floor of 4 (~10k LOC)",
    "state": "blocked",
    "acceptanceCriteria": [
      {
        "text": "regression.command and regression.authorCommand are configured (node --test .chalk/held-out + a BYO claude author); .chalk/held-out stays gitignored"
      },
      {
        "text": "chalk guard gen authors at least 4 held-out test files derived from the spec (the size floor for ~10k LOC), locked into regression.tests"
      },
      {
        "text": "chalk audit runs GREEN with the understaffing warning gone"
      },
      {
        "text": "implementer blindness holds: the tests are written by the BYO guard author subprocess; this session never reads .chalk/held-out contents"
      }
    ],
    "tests": [],
    "heldOut": [],
    "after": [],
    "issue": {
      "number": 92,
      "url": "https://github.com/chalkagents/chalk-protocol/issues/92",
      "body": "`chalk audit` is GREEN but warns:\n\n> ⚠ held-out set (0) is below the size floor (4 for 9919 LOC) — P7 stringency scales with code; author more via `chalk guard`.\n\nDogfooding gap: the protocol's own P7 lever is unarmed. Author 4+ held-out tests from the spec's durable criteria (`chalk guard gen` can draft them), keep `.chalk/held-out/` gitignored per the doctor rule.\n\n_filed during the 2026-07-06 issue sweep_"
    },
    "branchType": "feat",
    "labels": [
      "help wanted"
    ],
    "pipeline": {
      "stage": "selected",
      "at": "2026-07-06T08:12:17.234Z"
    },
    "createdAt": "2026-07-06T08:12:17.234Z",
    "reviews": [],
    "startedAt": "2026-07-06T08:54:03.352Z",
    "blockedFrom": "in-progress",
    "block": {
      "needs": "human-input",
      "reason": "The held-out set must be authored OUTSIDE this session (implementer blindness): the sandbox classifier refuses to let this agent spawn an auto-writing author into .chalk/held-out. Human steps: run 'chalk guard gen' in your own terminal (authorCommand is configured), then set protocol.regression.command='node --test .chalk/held-out', run 'chalk audit' (expect GREEN, floor 5 for ~10k LOC), and 'chalk unblock task-2cae7c80'.",
      "at": "2026-07-06T09:07:43.229Z"
    },
    "handoff": {
      "path": ".chalk/handoffs/task-2cae7c8-1.md",
      "at": "2026-07-06T09:07:43.267Z",
      "reason": "human-input",
      "seq": 1
    }
  },
  {
    "id": "task-f1308f82",
    "title": "fix: release --commit partial-failure recovery — a post-commit tag failure leaves an untagged release commit, and a re-run version-skips",
    "state": "done",
    "acceptanceCriteria": [
      {
        "text": "On a post-commit tag failure, either roll the release commit back (`git reset --hard HEAD~1` is safe — the commit contains only the release artifacts) or make the re-run detect the orphaned release commit and tag it instead of re-bumping"
      },
      {
        "text": "Test the recovery path (tag fails once after the commit → next run converges on ONE release commit + tag)"
      },
      {
        "text": "While there: cover `--commit --no-tag` (commits the bump, skips the probe) with a test"
      }
    ],
    "tests": [
      {
        "path": "test/release-recovery.test.mjs",
        "sha256": "cf95ca1bb156c91210a523c2eb13574b40bdb2250eb7379aa555fb6f50ee4619"
      }
    ],
    "heldOut": [],
    "after": [],
    "issue": {
      "number": 91,
      "url": "https://github.com/chalkagents/chalk-protocol/issues/91",
      "body": "From the task-021f498 adversarial review (low, non-blocking, shipped as-is).\n\n`chalk release --commit` probes the tag collision up front, but if `git tag` fails AFTER the release commit for a non-collision reason (hook, perms, probe→tag race), `die()` leaves an untagged `chore(release)` commit at HEAD with the bumped package.json. A re-run then bumps FROM the already-bumped version (version skip) and produces a second release commit.\n\n- [ ] On a post-commit tag failure, either roll the release commit back (`git reset --hard HEAD~1` is safe — the commit contains only the release artifacts) or make the re-run detect the orphaned release commit and tag it instead of re-bumping\n- [ ] Test the recovery path (tag fails once after the commit → next run converges on ONE release commit + tag)\n- [ ] While there: cover `--commit --no-tag` (commits the bump, skips the probe) with a test\n\n_filed from the chalk review of task-021f498_"
    },
    "branchType": "feat",
    "labels": [
      "enhancement"
    ],
    "pipeline": {
      "stage": "cleaned",
      "at": "2026-07-06T08:30:15.571Z"
    },
    "createdAt": "2026-07-06T08:12:17.234Z",
    "reviews": [
      {
        "at": "2026-07-06T08:19:01.652Z",
        "by": "adversary",
        "verdict": "block",
        "findings": [
          {
            "severity": "high",
            "area": "regression",
            "note": "The resume detector cannot distinguish an INTERRUPTED --commit run from a completed, INTENTIONAL `--commit --no-tag` release — which is now this repo's own release path (dev/main promote flow, #98/decision evt-0e49e19e: `release --commit --no-tag` on dev, tag lands on main's tip). After such a release, HEAD on dev is a `chore(release): vX` commit whose tag is legitimately absent locally (never created there, or on main and not yet fetched). The next `release --commit` with new done tasks then falsely 'resumes': it tags vOLD onto dev's commit and/or marks the NEW tasks released at the OLD version with no changelog entry and no bump — new work is silently swallowed. No test covers 'completed --no-tag release, then a subsequent release'; test 2 only covers the tagged-success case."
          },
          {
            "severity": "med",
            "area": "correctness",
            "note": "On resume, `for (const t of tasks) t.released = v` marks ALL currently releasable tasks released at the orphan version — including tasks that became done AFTER the interrupted run. Those tasks are absorbed into a v that already has a frozen CHANGELOG entry not mentioning them, so they never appear in any release notes. The interrupted run's task set should be what gets marked (or later arrivals excluded); untested."
          },
          {
            "severity": "med",
            "area": "correctness",
            "note": "Orphan detection inspects only HEAD's subject (`log -1`). If any commit lands on top of the orphaned release commit before the re-run — routine on a shared dev branch or under `chalk run` — the recovery is skipped and the original bug (bump-from-bumped version skip + stacked release commit) returns. Criterion 1 is satisfied only for an immediate re-run with an untouched HEAD; this limitation is neither handled nor pinned by a test. Also `--dry-run` returns before the recovery block, so a post-interruption dry-run previews the wrong, double-bumped version."
          },
          {
            "severity": "low",
            "area": "test-adequacy",
            "note": "Test 1 is genuinely non-vacuous (the refs/tags/v0.1.0/block injection is a real post-commit tag failure, and the convergence assertions fail if the recovery is reverted), and test 2 guards false-resume after a tagged success — good. But test 3 (--no-tag) stops at the first release; nothing asserts that an intentional --no-tag release commit at HEAD doesn't poison a later run, which is exactly the high-severity gap above."
          },
          {
            "severity": "low",
            "area": "design-intent",
            "note": "The diff bundles unrelated spine changes — a new `protocol.worktree` block in .chalk/chalk.json and two unrelated imported tasks (#98, #92) — against the repo rule 'keep diffs small and scoped to the current task'. Not blocking on its own, but it widens the review surface of a release-safety fix."
          }
        ]
      },
      {
        "at": "2026-07-06T08:25:16.551Z",
        "by": "adversary",
        "verdict": "pass",
        "findings": [
          {
            "severity": "med",
            "area": "regression",
            "note": "The completion marker (`Released vX` in .chalk/decisions.md) is written to the WORKING TREE only — the release commit itself contains just CHANGELOG.md + package.json. After an intentional `--commit --no-tag` release, if the decisions.md change is lost before the next run (fresh clone, new worktree, checkout/reset, or the spine push not yet landed), the completed release commit again looks like an orphan and the false-resume the prior review blocked (tag vOLD, swallow new work) returns. The locked test only exercises the same-working-tree case; in-repo flows push the spine so this is an edge, but it is the one hole left in the discriminator."
          },
          {
            "severity": "low",
            "area": "test-adequacy",
            "note": "The new dry-run resume branch (line 509: post-interruption `--dry-run` previews 'would RESUME' instead of a double-bumped version — explicitly called out in the prior review) has no test; reverting just that line leaves the suite green. Also nothing pins the resume-fails-again path (tag fails a second time → 'Nothing was marked released')."
          },
          {
            "severity": "low",
            "area": "correctness",
            "note": "Orphan detection scans only `log -50`; an orphan buried >50 commits deep (plausible under `chalk run` on a busy dev branch) silently reverts to the original bump-from-bumped behavior. Likewise a plain `release` (no --commit) run after an interruption bypasses the recovery block entirely and tags HEAD at a skipped version — outside the stated criteria but an adjacent trap. If the orphan's %cI can't be parsed, cutoff=0 defers ALL tasks and tags an empty release."
          },
          {
            "severity": "low",
            "area": "correctness",
            "note": "The completion check is a substring regex over all of decisions.md — any decision title/why that happens to contain `Released vX.Y.Z` for the orphan's version (e.g. a forced/manual note about that version) marks it completed and suppresses a legitimate resume. Narrow, but the marker is not namespaced."
          },
          {
            "severity": "low",
            "area": "design-intent",
            "note": "The diff again bundles unrelated spine changes (new `protocol.worktree` config block, imported tasks #98/#92, board entries) against the 'keep diffs small and scoped' rule. The locked-test amendment itself went through the sanctioned path (amend-spec decision logged, sha256 updated), and all four previously blocking findings are now each pinned by a dedicated locked test (decision marker, --no-tag non-orphan, buried orphan, late-arrival deferral), so this is noise, not a gate."
          }
        ]
      },
      {
        "at": "2026-07-06T08:25:52.786Z",
        "by": "amend-spec",
        "verdict": "stale",
        "note": "locked test amended after review — re-review required"
      },
      {
        "at": "2026-07-06T08:28:08.280Z",
        "by": "adversary",
        "verdict": "pass",
        "findings": [
          {
            "severity": "med",
            "area": "test-adequacy",
            "note": "Criterion 3's parenthetical 'skips the probe' is not directly pinned: test 3 ('--commit --no-tag') runs in a repo with no colliding tag, so it would still pass if the up-front collision probe ran under --no-tag. The observable of probe-skipping (a pre-existing v0.1.0 tag does not abort a --no-tag release) is unasserted. Non-blocking because the probe-guard (`wantTag && withCommit` at line 539) is pre-existing behavior, not part of this change, and the criterion's main clause — cover the --commit --no-tag flow — is met (commit exists, no tag, released marked, bump landed)."
          },
          {
            "severity": "med",
            "area": "regression",
            "note": "The completion marker ('Released vX' in .chalk/decisions.md) still lives outside the release commit (which contains only CHANGELOG.md + package.json). If the decisions.md append is lost before the next run (fresh clone/worktree, reset, spine push not landed), a completed --no-tag release commit re-qualifies as an orphan and the false-resume returns. The locked tests only exercise the same-working-tree case. In-repo flows commit/push the spine, so this is an accepted edge carried over from the prior pass review, not a new defect."
          },
          {
            "severity": "low",
            "area": "test-adequacy",
            "note": "The resume-fails-again path (tag fails a SECOND time during resume → die 'Nothing was marked released', line 512) is untested; deleting that catch leaves the suite green (crash vs. clean die is indistinguishable to the tests). The dry-run resume preview flagged last round IS now pinned and non-vacuous (test 5 lines 112–116: reverting line 509 yields 'release v0.2.0 (dry-run)', which fails both /would RESUME/ and doesNotMatch(/v0\\.2\\.0/))."
          },
          {
            "severity": "low",
            "area": "correctness",
            "note": "Residual edges, all outside the stated criteria: orphan scan depth is `log -50` (an orphan buried deeper silently reverts to bump-from-bumped); the completion check is an un-namespaced substring regex over all of decisions.md (a manual note containing 'Released vX.Y.Z' suppresses a legitimate resume); an unparseable %cI yields cutoff=0, deferring ALL tasks into an empty resumed release; a plain `release` (no --commit) after an interruption bypasses the recovery block entirely."
          },
          {
            "severity": "low",
            "area": "design-intent",
            "note": "The diff bundles unrelated spine changes (new `protocol.worktree` config block, imported tasks #98/#92, board entries) against the 'keep diffs small and scoped' rule — noise, not a gate. The amend-spec path was used correctly (two amendment decisions logged, sha256 updated in tasks.json), and each previously blocking finding — decision-marker discriminator, intentional --no-tag non-orphan, buried orphan, late-arrival deferral, dry-run resume preview — is now pinned by a dedicated locked test that fails if the corresponding behavior is reverted. All three acceptance criteria are substantively satisfied."
          }
        ]
      }
    ],
    "startedAt": "2026-07-06T08:14:39.294Z",
    "branch": "feat/91-fix-release-commit-partial-failure",
    "pr": {
      "number": 100,
      "url": "https://github.com/chalkagents/chalk-protocol/pull/100",
      "recorded": true,
      "lgtm": true
    },
    "doneAt": "2026-07-06T08:30:15.570Z"
  },
  {
    "id": "task-124fde8a",
    "title": "fix: chalk review advances pipeline.stage to 'reviewed' even when no PR exists — manual-order review pollutes the commit/pr stage guards",
    "state": "done",
    "acceptanceCriteria": [
      {
        "text": "`chalk review` should only advance the stage when the task is actually past pr-open (or track review separately from the pipeline progression)"
      },
      {
        "text": "`chalk pr`'s already-open path should not report success when `t.pr` is null/number-less"
      }
    ],
    "tests": [
      {
        "path": "test/review-stage-order.test.mjs",
        "sha256": "cd300c78dfb25e906cb32eacc9215bc8a2c95b31a4e32c1c1ae8d48129071896"
      }
    ],
    "heldOut": [],
    "after": [],
    "issue": {
      "number": 102,
      "url": "https://github.com/chalkagents/chalk-protocol/issues/102",
      "body": "Manual mode encourages review as soon as verify is green — often BEFORE `chalk commit`/`chalk pr`. But `chalk review` sets `pipeline.stage='reviewed'` (rank 6), so the later `chalk commit` (rank 4) and `chalk pr` (rank 5) no-op via their stage guards ('already done') even though nothing was committed and no PR exists — `chalk pr` even prints `PR #?`.\n\n- [ ] `chalk review` should only advance the stage when the task is actually past pr-open (or track review separately from the pipeline progression)\n- [ ] `chalk pr`'s already-open path should not report success when `t.pr` is null/number-less\n\n_hit live on task-f1308f82 (#91); worked around by rewinding the stage marker_"
    },
    "branchType": "feat",
    "labels": [
      "enhancement"
    ],
    "pipeline": {
      "stage": "cleaned",
      "at": "2026-07-06T09:26:22.752Z"
    },
    "createdAt": "2026-07-06T09:17:15.836Z",
    "reviews": [
      {
        "at": "2026-07-06T09:22:15.297Z",
        "by": "adversary",
        "verdict": "pass",
        "findings": [
          {
            "severity": "med",
            "area": "test-adequacy",
            "note": "The stage-advance guard is duplicated on two paths — manual --note (bin/chalk.mjs:1352) and adversary reviewer (bin/chalk.mjs:1374) — but the locked test only exercises the manual path (no reviewer configured). Reverting line 1374 alone would still pass the suite; a stub reviewer command (e.g. node -e emitting a pass verdict JSON) could cover the adversary path in a follow-up (non-locked) test."
          },
          {
            "severity": "low",
            "area": "regression",
            "note": "A legacy task with stage=pr-open but a number-less t.pr (e.g. gh output that didn't match /pull/(\\d+)/ at pr-create time, line 410) now falls through the guard and re-runs push + `gh pr create`, which will die with 'a pull request already exists' instead of the old (false) success. This is what criterion 2 demands, but the recovery is a hard die rather than re-resolving the existing PR number — an operator hitting this must fix t.pr by hand."
          },
          {
            "severity": "low",
            "area": "correctness",
            "note": "After a manual-order pass (verdict recorded, stage stays 'branched'), the idempotency guard at line 1342 (stageDone 'reviewed') no longer short-circuits, so re-running `chalk review` pre-PR appends duplicate review entries / re-invokes the reviewer. Converges harmlessly (done/merge gates read only the last verdict), but slightly weakens the 'don't append a DUPLICATE review' intent documented at lines 1337–1341."
          },
          {
            "severity": "low",
            "area": "design-intent",
            "note": "Criterion 1's chosen branch ('track review separately') means a review that passed against the pre-commit tree still satisfies the merge gate later — merge (line 744) even backfills the LGTM onto a PR the reviewer never saw. Consistent with the criterion as written and with pre-existing behavior, but worth noting the reviewed artifact and the merged artifact can differ in manual order."
          }
        ]
      },
      {
        "at": "2026-07-06T09:22:47.010Z",
        "by": "amend-spec",
        "verdict": "stale",
        "note": "locked test amended after review — re-review required"
      },
      {
        "at": "2026-07-06T09:24:56.856Z",
        "by": "adversary",
        "verdict": "pass",
        "findings": [
          {
            "severity": "low",
            "area": "test-adequacy",
            "note": "Coverage of the change is now genuine and revert-detecting: test 1 pins the manual-path guard (bin/chalk.mjs:1352), test 3 pins the adversary-path guard (line 1374) via a stub reviewer — closing the revertible-line gap flagged in the prior review — and tests 4/5 pin the `t.pr?.number` requirement in the `chalk pr` idempotency guard (line 383) plus the legitimate short-circuit and `recorded` backfill. Reverting any of the three changed guards fails a locked assertion. The amendment went through the sanctioned amend-spec path (prior pass correctly marked stale)."
          },
          {
            "severity": "low",
            "area": "correctness",
            "note": "After a manual-order pass the stage stays 'branched', so the idempotency short-circuit at line 1342 (requires stage>=reviewed) no longer stops a pre-PR re-run of `chalk review`: it appends a duplicate review entry / re-invokes the reviewer. Converges harmlessly — done/merge gates read only the last verdict (line 744) — and once the PR opens a re-run advances the stage, so nothing strands. Acceptable residue of the 'track review separately' branch of criterion 1."
          },
          {
            "severity": "low",
            "area": "regression",
            "note": "A legacy task at stage=pr-open with a number-less t.pr (gh output that missed /pull\\/(\\d+)/ at create time, line 410) now falls through the guard and re-runs push + `gh pr create`, which dies with 'a pull request already exists' instead of the old false success. This is exactly what criterion 2 demands, but recovery requires an operator to fix t.pr.number by hand rather than the code re-resolving the existing PR. Merge itself was already hard-gated on t.pr?.number (line 736), so no new stranding — the lie just surfaces earlier and honestly."
          },
          {
            "severity": "low",
            "area": "design-intent",
            "note": "With 'track review separately' chosen, a review that passed against the pre-commit tree still satisfies the merge gate later (line 744 even backfills the LGTM onto a PR the reviewer never saw). Consistent with the criterion as written and with pre-existing behavior, but the reviewed artifact and merged artifact can differ in manual order. Also: the diff bundles two unrelated newly-specced tasks (task-10382dc9, task-a8aa7559) into .chalk/tasks.json/board — inert queue metadata from issue intake, not behavior, but slightly outside the 'diffs scoped to the current task' rule."
          }
        ]
      }
    ],
    "startedAt": "2026-07-06T09:18:00.361Z",
    "branch": "feat/102-fix-chalk-review-advances-pipeline",
    "pr": {
      "number": 104,
      "url": "https://github.com/chalkagents/chalk-protocol/pull/104",
      "recorded": true,
      "lgtm": true
    },
    "doneAt": "2026-07-06T09:26:22.751Z"
  },
  {
    "id": "task-10382dc9",
    "title": "chore: dogfood — make chalk's own loop (issue pull → autopilot → retro) the default way chalk contributes to chalk",
    "state": "done",
    "acceptanceCriteria": [
      {
        "text": "A documented sweep runbook in the repo (CONTRIBUTING.md or RUNNING-AUTONOMOUSLY.md): issue pull → spec → autopilot → retro, runnable on this repo as-is with the committed `.chalk/chalk.json` agent config."
      },
      {
        "text": "`chalk issue pull` → pipeline → merge demonstrably works end-to-end on this repo: at least one real issue from the open backlog lands through it, and the PR/task cross-references the issue."
      },
      {
        "text": "Retro runs at the end of a sweep and files friction it finds (already wired in autopilot — criterion is that a chalk-on-chalk sweep exercises it and the filed issues reference the dogfood run)."
      },
      {
        "text": "The dogfooding claim is auditable: some command or doc (e.g. `chalk stats`, once #78 lands) can show what fraction of merged PRs went through the gate vs bypassed it."
      }
    ],
    "tests": [],
    "heldOut": [],
    "after": [],
    "issue": {
      "number": 101,
      "url": "https://github.com/chalkagents/chalk-protocol/issues/101",
      "body": "## Problem\n\n\"chalk builds itself\" is the first line of CONTRIBUTING.md, and it's *mostly* true — the `.chalk/` spine is live, and the 2026-07-06 issue sweep (PRs #93–#96) landed entirely through the pipeline's merge gate. But dogfooding today is ad hoc: it happens when a session chooses to drive the loop, not because the repo's default contribution path *is* the loop. That leaves value on the table twice over:\n\n1. **Credibility** — the strongest evidence chalk works is that chalk ships chalk. Right now that claim is anecdotal (buried in commit history) rather than demonstrated and measurable.\n2. **Feedback pressure** — every gap in the harness (context too thin, gate too slow, block reason unhelpful) is felt first by chalk-on-chalk. If contributions can silently bypass the protocol, the friction that should become issues never gets logged.\n\n## What \"dogfooded\" should mean here\n\nThe full loop, not just the manual per-task loop CONTRIBUTING.md already documents:\n\n- **Intake:** GitHub issues are the backlog; `chalk issue pull` turns them into tasks. Specing (criteria + locked tests) is part of intake, not an afterthought — a pulled task that can't be specced is itself a friction finding.\n- **Execution:** sweeps run through the pipeline (`chalk branch → run → pr → merge`), driven by the configured executor/planner/reviewer agents — `chalk autopilot` as the standard sweep entrypoint rather than a hand-driven session.\n- **Close the loop:** every sweep ends in `chalk retro` — lessons appended, friction auto-filed as new issues. Dogfooding is the primary *source* of harness-improvement issues, not just a consumer of them.\n- **Bypass is visible:** hand-commits to dev that skip the gate should be detectable (overlaps #79 tamper-evident spine; #78 `chalk stats` is the natural place to report gate-vs-bypass ratio).\n\n## Acceptance criteria\n\n- [ ] A documented sweep runbook in the repo (CONTRIBUTING.md or RUNNING-AUTONOMOUSLY.md): issue pull → spec → autopilot → retro, runnable on this repo as-is with the committed `.chalk/chalk.json` agent config.\n- [ ] `chalk issue pull` → pipeline → merge demonstrably works end-to-end on this repo: at least one real issue from the open backlog lands through it, and the PR/task cross-references the issue.\n- [ ] Retro runs at the end of a sweep and files friction it finds (already wired in autopilot — criterion is that a chalk-on-chalk sweep exercises it and the filed issues reference the dogfood run).\n- [ ] The dogfooding claim is auditable: some command or doc (e.g. `chalk stats`, once #78 lands) can show what fraction of merged PRs went through the gate vs bypassed it.\n\n## Relationship to open issues\n\n#78 (stats) and #79 (tamper-evidence) supply the measurement/enforcement pieces; #99 (token ledger) makes each dogfood sweep's cost visible. This issue is the umbrella that makes the loop the *default path*, and the others its instruments.\n\n## Non-goals\n\n- Forbidding manual work entirely — humans and interactive sessions still use the manual loop CONTRIBUTING.md describes; the point is that both paths go through the same gates and bypasses are visible, not that only autopilot may commit.\n"
    },
    "branchType": "feat",
    "labels": [
      "enhancement"
    ],
    "pipeline": {
      "stage": "cleaned",
      "at": "2026-07-06T11:57:30.143Z"
    },
    "createdAt": "2026-07-06T09:17:15.842Z",
    "reviews": [
      {
        "at": "2026-07-06T11:49:31.143Z",
        "by": "adversary",
        "verdict": "block",
        "findings": [
          {
            "severity": "high",
            "area": "correctness",
            "note": "The diff contains only .chalk spine bookkeeping (task-10382dc9 moved to in-progress, branch/PR #112 recorded, board/plan/updates sync). No criterion is satisfied BY this change: the runbook (criterion 1) pre-exists in RUNNING-AUTONOMOUSLY.md from PR #108, the issue-pull→merge demonstrations (criterion 2) are prior PRs #100/#103/#104/#105, the retro evidence (criterion 3) is the prior #107 filing, and the stats capability (criterion 4) landed in PR #109. Reverting this diff changes nothing about whether the criteria hold — it is state-recording, not delivery."
          },
          {
            "severity": "high",
            "area": "correctness",
            "note": "Criterion 2 requires the loop to work 'end-to-end ... merge': the recorded pipeline stage for this task is 'pr-open' (PR #112 open, not merged), and the task state is 'in-progress'. The change under review is a mid-flight snapshot; nothing demonstrates a merge through the gate for this dogfood run itself."
          },
          {
            "severity": "high",
            "area": "test-adequacy",
            "note": "Zero locked tests and zero new tests or verifiable artifacts. Criterion 4 demands the dogfooding claim be auditable, yet the diff includes no `chalk stats` output, no doc citing the gate-vs-bypass fraction, and no artifact a reviewer or CI could check. Every claim of satisfaction rests on out-of-band prior PRs and a handoff note — unverifiable from this change. This is a vacuous pass by construction."
          },
          {
            "severity": "med",
            "area": "correctness",
            "note": "Criterion 3 requires filed retro issues to 'reference the dogfood run'. No evidence in the diff (or updates.jsonl entries added by it) that a retro executed at the end of THIS sweep or that any filed issue references it; the handoff's mention of #107 predates this change and its cross-reference to the dogfood run is unverified."
          },
          {
            "severity": "med",
            "area": "design-intent",
            "note": "The task's intent is to make the chalk-on-chalk loop the demonstrated default contribution path. Recording a testArtifact/PR pointer on the board card is meta-evidence at best; the evidenceUrl points to source issue #101, not to run evidence (retro output, stats report, or merged-PR trail). The change documents an in-progress claim rather than completing the contract."
          },
          {
            "severity": "low",
            "area": "regression",
            "note": "Spine files (tasks.json, board, updates.jsonl) are hand-consistent in this diff, but the plan file was deleted from .chalk/plans/todo/ while a copy now lives under .chalk/plans/inprogress/ outside the diff — if that move was not generated by `chalk plans`, spine/plan drift is possible; ensure regeneration rather than manual edits."
          }
        ]
      },
      {
        "at": "2026-07-06T11:52:38.436Z",
        "by": "adversary",
        "verdict": "block",
        "findings": [
          {
            "severity": "high",
            "area": "correctness",
            "note": "Criterion 3 is not demonstrably met: it requires a chalk-on-chalk sweep to exercise retro AND the filed friction issues to 'reference the dogfood run'. The only retro artifact traceable in the spine is issue #107 (imported at evt-ca135b5e, a spec-lock/untracked-test bug). Nothing in this diff, updates.jsonl, or the docs shows #107 (or any retro-filed issue) actually cross-referencing the dogfood run — the claim lives only in the handoff's prose ('lessons + #107 filed'), which is self-attestation, not evidence."
          },
          {
            "severity": "med",
            "area": "correctness",
            "note": "Criterion 2 says the loop must work 'end-to-end ... merge'. This task's own pipeline stage is 'pr-open' (PR #112, unmerged) — expected mid-review, but the fallback evidence (prior PRs #100/#103–#105/#109 cross-referencing issues #91/#98/#102/#99/#78) is asserted in CONTRIBUTING.md and spine event titles rather than shown: no in-repo artifact (e.g. a captured pipeline/evidence trail or stats output) lets a reviewer confirm the PR↔issue cross-references without trusting GitHub-side state."
          },
          {
            "severity": "med",
            "area": "test-adequacy",
            "note": "The substantive delivery in this diff is a CONTRIBUTING.md paragraph pointing at `chalk stats` for criterion 4. `chalk stats` does exist (bin/chalk.mjs:896, lib/stats.mjs, landed via PR #109 with its own locked test), so the capability is real — but no artifact in this change shows stats actually run on this repo reporting the gate-vs-bypass fraction. Criterion 4 asks that the claim 'can show' the fraction; the diff documents that it could, without a single receipt (no sample output, no CI check, no doc citing the current numbers). Reverting this diff would fail no test and no gate."
          },
          {
            "severity": "med",
            "area": "design-intent",
            "note": "The task's goal is to make the dogfood loop the demonstrated default contribution path. Criteria 1 (runbook in CONTRIBUTING.md §'The dogfood sweep' + RUNNING-AUTONOMOUSLY.md, landed in PR #108) and the stats capability (PR #109) pre-exist this change; this diff contributes only the stats line in the runbook, the receipts paragraph, and spine bookkeeping for the in-flight run. That is a plausible closing move for a dogfood chore, but the prior adversarial review's core blocking findings (unverified retro cross-reference, unmerged end-to-end demonstration for this run) are recorded in tasks.json and are not resolved by this diff — it largely re-submits the same state plus documentation."
          },
          {
            "severity": "low",
            "area": "regression",
            "note": "No behavioral risk: changes are docs (CONTRIBUTING.md) and .chalk spine state (board/tasks/plan/updates), which appear internally consistent (started→branched→committed→PR#112→review events align with tasks.json pipeline stage 'pr-open' and the plan file's todo→inprogress move). Verify that the plan-file rename was generated by chalk rather than hand-moved to avoid spine drift."
          }
        ]
      },
      {
        "at": "2026-07-06T11:57:11.597Z",
        "by": "adversary",
        "verdict": "pass",
        "findings": [
          {
            "severity": "med",
            "area": "correctness",
            "note": "Criterion 2's end-to-end merge for THIS run is still open (task-10382dc9 pipeline stage 'pr-open', PR #112 unmerged) — inherent to a self-closing dogfood chore, and acceptable only because the criterion ('at least one real issue lands through it') is verifiably met by prior runs: spine + archive contain 18+ tasks at pipeline stage 'cleaned' with pr numbers and issue cross-references (e.g. #78→PR #109, #107 imported as task-8189ac26), matching the documented receipts. chalk done/merge gates will enforce #112's own landing."
          },
          {
            "severity": "low",
            "area": "correctness",
            "note": "Criterion 3 verified in-repo this round: issue #107's full body is recorded in .chalk/tasks.json (task-8189ac26.issue.body), opens 'Three reviews in this sweep (release --commit, config-drift gate, evidence-push)...' and carries the retro signature that bin/chalk.mjs:839 actually appends ('_filed by `chalk retro` (self-healing)_'). It references the dogfood sweep by content rather than by a run id/date — a link to the run's date or spine events would make the cross-reference unambiguous."
          },
          {
            "severity": "low",
            "area": "test-adequacy",
            "note": "The diff is docs + spine bookkeeping, so no new behavior lacks a test (the hard rule is not triggered). Criterion 4's capability is independently pinned: `chalk stats` exists (bin/chalk.mjs:896, lib/stats.mjs) with its own locked test from PR #109; the CONTRIBUTING snapshot format matches the live renderer line-for-line (landing/gated/overridden/pipeline), the '2 overridden' matches the two --force-review decisions in .chalk/decisions.md, and 'pipeline 19/59' matches the ~19 'cleaned'-stage tasks countable in tasks.json + archive. The snapshot will drift over time, but the doc explicitly names the command, not the snapshot, as the source of truth."
          },
          {
            "severity": "low",
            "area": "regression",
            "note": "No behavioral risk: CONTRIBUTING.md prose plus internally consistent spine state (started→branched→committed→PR #112→review events align with tasks.json stage 'pr-open' and the plan file's todo→inprogress move). Criterion 1's 'runnable as-is' claim checks out: committed .chalk/chalk.json wires executor/planner/reviewer/retro commands."
          }
        ]
      }
    ],
    "handoff": {
      "path": ".chalk/handoffs/task-10382dc-1.md",
      "at": "2026-07-06T09:59:22.328Z",
      "reason": "upstream",
      "seq": 1
    },
    "startedAt": "2026-07-06T11:46:02.536Z",
    "branch": "feat/101-chore-dogfood-make-chalk-s",
    "pr": {
      "number": 112,
      "url": "https://github.com/chalkagents/chalk-protocol/pull/112",
      "recorded": true,
      "lgtm": true
    },
    "doneAt": "2026-07-06T11:57:30.142Z"
  },
  {
    "id": "task-a8aa7559",
    "title": "feat: token-level cost ledger — record usage per agent call so chalk's induced overhead (and savings) are measurable",
    "state": "done",
    "acceptanceCriteria": [
      {
        "text": "Agent calls through a claude-shaped command record `tokens`, `costUsd`, and `turns` in `.chalk/local/cost.jsonl`; non-claude runners still record ms-only, and a malformed/missing envelope never fails the stage."
      },
      {
        "text": "The executor invocation in `chalk run` captures usage without losing live terminal output."
      },
      {
        "text": "Injecting `--output-format json` does not break the reviewer/retro/discovery/feedback JSON parsing (envelope is unwrapped before the existing parsers see it), and commands that already pin `--output-format` are left untouched."
      },
      {
        "text": "`chalk cost` shows per-stage/per-task tokens, overhead share, and tokens-per-accepted-task, and still renders correctly against a ledger containing only legacy ms-only records."
      }
    ],
    "tests": [
      {
        "path": "test/cost-ledger.test.mjs",
        "sha256": "7616e231580f87e2fff59c33b5691631802f2b8145ce37a0fbc06190ec5819cd"
      }
    ],
    "heldOut": [],
    "after": [],
    "issue": {
      "number": 99,
      "url": "https://github.com/chalkagents/chalk-protocol/issues/99",
      "body": "## Problem\n\nChalk's pitch is that the harness pays for itself — gates catch churn early, context injection avoids re-derivation. But we can't back that claim (or refute it) because the cost ledger records only *that* an agent was called, not *what it consumed*.\n\nToday `store.logCost()` (`lib/store.mjs:193`) appends `{at, taskId, stage, agent, ms}` per BYO agent call, and `chalk cost` summarizes calls + wall-clock. The comment above it explicitly punts on tokens. Meanwhile every configured stage runs `claude -p`, and `claude -p --output-format json` returns an envelope with `usage` (input / output / cache-read / cache-write tokens), `total_cost_usd`, and `num_turns` — the data is one flag away and we drop it on the floor.\n\n## What token visibility buys\n\nSavings are a counterfactual — chalk can't measure them directly, only make the comparison cheap:\n\n1. **Overhead share** — fraction of tokens spent on gates (plan + review + retro) vs the work stage. The \"cost of the harness\" number.\n2. **Tokens per accepted task** — unit economics including retries. A verify-RED loop that burns 3 executor attempts becomes visible instead of anecdotal.\n3. **Baseline experiment (enabled, not built)** — run the same task set executor-only vs full chalk and compare tokens-per-accepted-task + defect escape rate. A one-off study on top of the ledger, but the ledger has to record tokens first.\n\n## Design sketch\n\n**Capture (adapter-shaped, BYO-safe):**\n- If a stage command is claude-shaped (`claude -p ...` without an explicit `--output-format`), inject `--output-format json`, unwrap the envelope: harvest `usage` / `total_cost_usd` / `num_turns`, pass the inner `result` to the existing parsers (reviewer/retro/discovery/feedback already pipe + parse stdout, e.g. `lib/review.mjs:99`).\n- Opportunistic fallback: if the command isn't recognized but stdout parses as a claude envelope, still harvest it.\n- opencode runner: harvest its usage output if available; unknown runners fall back to today's ms-only record.\n- Accounting must never fail a run — parse errors degrade to ms-only, silently correct rather than loudly broken.\n\n**Executor call site (the structural bit):** `lib/run.mjs:24` runs the executor with `stdio: ['pipe','inherit','inherit']`, so stdout is never captured. Switch to capturing stdout while preserving terminal visibility (tee/stream + re-emit), then harvest usage the same way.\n\n**Ledger + report:**\n- Extend `logCost` records with optional `tokens: {in, out, cacheRead, cacheWrite}`, `costUsd`, `turns`. Old records stay valid.\n- `chalk cost` reports per-stage and per-task token totals, overhead share (gate tokens vs work tokens), tokens-per-accepted-task, and `costUsd` totals when present — degrading gracefully to the calls+wall-clock view for records without tokens.\n\n## Acceptance criteria\n\n- [ ] Agent calls through a claude-shaped command record `tokens`, `costUsd`, and `turns` in `.chalk/local/cost.jsonl`; non-claude runners still record ms-only, and a malformed/missing envelope never fails the stage.\n- [ ] The executor invocation in `chalk run` captures usage without losing live terminal output.\n- [ ] Injecting `--output-format json` does not break the reviewer/retro/discovery/feedback JSON parsing (envelope is unwrapped before the existing parsers see it), and commands that already pin `--output-format` are left untouched.\n- [ ] `chalk cost` shows per-stage/per-task tokens, overhead share, and tokens-per-accepted-task, and still renders correctly against a ledger containing only legacy ms-only records.\n\n## Non-goals\n\n- The executor-only vs full-chalk baseline study itself (separate experiment once this lands).\n- Pricing tables / cost estimation beyond what the CLI already reports (`total_cost_usd` is authoritative enough; subscription users read tokens, API users read USD).\n"
    },
    "branchType": "feat",
    "labels": [
      "enhancement"
    ],
    "pipeline": {
      "stage": "cleaned",
      "at": "2026-07-06T09:53:35.095Z"
    },
    "createdAt": "2026-07-06T09:17:15.843Z",
    "reviews": [
      {
        "at": "2026-07-06T09:37:02.442Z",
        "by": "adversary",
        "verdict": "block",
        "findings": [
          {
            "severity": "med",
            "area": "test-adequacy",
            "note": "Criterion 3 names FOUR parsers — reviewer/retro/discovery/feedback — plus the plan stage got the same injection+unwrap wiring (bin/chalk.mjs:429-432, lib/retro.mjs:40-43, lib/discovery.mjs:39-42, lib/feedback.mjs:44-47), but the locked suite integration-tests only the reviewer path (opportunistic unwrap) and the executor path. Reverting the retro, discovery, feedback, or plan call-sites individually (back to plain execSync + parseLastJson(raw), ms-only logCost) leaves every test green while silently violating criteria 1 and 3 for those stages. The helpers are unit-tested, but the wiring is the behavior — this is the same revertible-line gap the previous task's review flagged and that was ultimately pinned via amend-spec. At least one of retro/discovery/feedback should be exercised end-to-end with an envelope-wrapped payload (stub agent emitting ENVELOPE('{\"lessons\":[],\"issues\":[]}')) asserting both the parse survives and tokens land in the ledger."
          },
          {
            "severity": "low",
            "area": "correctness",
            "note": "isClaudeShaped tokenizes the raw string, so a runner-prefixed command (withRunner → 'fvm claude -p …', lib/config.mjs:24 applied BEFORE withJsonOutput at every gate site) is never detected → no injection, ms-only. Also a quoted argument containing '--output-format' (e.g. --append-system-prompt \"… --output-format …\") false-positives as pinned. Both degrade gracefully to legacy behavior rather than fail, so acceptable under criterion 1, but the dogfood claim 'every configured stage records tokens' quietly breaks the moment protocol.runner is set."
          },
          {
            "severity": "low",
            "area": "correctness",
            "note": "runExecutorCaptured pipes claude stdout through execSync's default 1 MiB maxBuffer (lib/cost.mjs:59 sets no maxBuffer). A long session envelope over 1 MiB throws ENOBUFS → raw = truncated e.stdout → unwrap fails → the operator sees a garbled partial JSON blob as the 'narrative' and the record is ms-only. Never fails the stage (criterion 1 holds) but criterion 2's 'without losing live terminal output' degrades to raw envelope fragments in exactly the long-run case where output matters most."
          },
          {
            "severity": "low",
            "area": "regression",
            "note": "In runReview, a claude reviewer that exits non-zero concatenates stdout+stderr (lib/review.mjs:102); with injection now on, the envelope+stderr mix fails parseEnvelope AND the verdict is JSON-escaped inside the envelope's result string, so parseLastJson cannot recover it → status:'error' where the pre-change text-mode output would have parsed. Narrow (claude -p normally exits 0), but it is a new failure mode introduced by the injection with no test."
          },
          {
            "severity": "low",
            "area": "design-intent",
            "note": "handoff (lib/handoff.mjs:43) and prbody (lib/prbody.mjs:23) agent calls got neither injection nor logCost, so claude-shaped calls there stay invisible to the ledger — defensible (their output is prose, not JSON), but criterion 1 says 'agent calls', so the overhead-share number `chalk cost` reports undercounts the harness. Also the diff again bundles unrelated queue metadata (task-10382dc9, the done task-124fde8a records) into .chalk/tasks.json/board — inert, but outside the 'diffs scoped to the current task' rule, same as flagged last review."
          }
        ]
      },
      {
        "at": "2026-07-06T09:41:24.040Z",
        "by": "adversary",
        "verdict": "block",
        "findings": [
          {
            "severity": "med",
            "area": "test-adequacy",
            "note": "Criterion 3 explicitly names discovery and feedback, but the locked suite never exercises `chalk discover` or `chalk feedback` through an envelope. The wirings in lib/discovery.mjs (lines 39–42) and lib/feedback.mjs (lines 44–47) are individually revertible: dropping withJsonOutput+unwrapAgentOutput from either file (back to bare execSync + parseLastJson) passes every locked test, since the unit tests only pin the lib/cost.mjs helpers, not these call sites. This is exactly the revertible-stage-wiring gap the prior review blocked on for retro/plan — the amendment closed those two but left discovery/feedback open."
          },
          {
            "severity": "med",
            "area": "test-adequacy",
            "note": "The --output-format injection half of criterion 1 is only pinned for the EXECUTOR stage (the fake-claude-on-PATH test). The review/retro/plan e2e tests use node stubs that self-emit envelopes, so they pin the unwrap but not the injection: reverting withJsonOutput at review.mjs:100, retro.mjs:40, or bin/chalk.mjs:429 (keeping unwrapAgentOutput) still passes the whole suite — a real claude-shaped reviewer/retro/planner would silently regress to ms-only records with no test failing."
          },
          {
            "severity": "med",
            "area": "test-adequacy",
            "note": "Two changes cited in the amend-spec decision note are NOT actually asserted by the locked test: (a) the stdout-first-on-nonzero-exit change in review.mjs:105 (previously stdout+stderr concatenated; test 3's stub exits 0, so the catch path is never exercised — reverting to concat passes), and (b) the 64MiB maxBuffer in runExecutorCaptured. Both are behavior-altering and revertible without a test failure."
          },
          {
            "severity": "low",
            "area": "correctness",
            "note": "parseEnvelope requires the ENTIRE stdout to be one JSON envelope (JSON.parse of trimmed raw). If an injected claude call prepends anything to stdout (warning line, MCP banner), the unwrap fails and the verdict/lessons JSON stays escaped inside the envelope's result string, where parseLastJson cannot recover it (escaped quotes break the balanced-brace parse) — the review/retro stage then errors. So 'a malformed envelope never fails the stage' holds for the ledger write but not for the downstream parse in this plausible edge; before this change no injection happened, so the parsers saw plain text."
          },
          {
            "severity": "low",
            "area": "regression",
            "note": "For a claude-shaped executor, runExecutorCaptured buffers stdout until the process exits (up to the 10-minute timeout) and re-emits the narrative afterward. Defensible for -p's single final blob (and stderr stays live), but 'without losing live terminal output' is satisfied only in the eventual-re-emit sense: an operator watching a long run sees no stdout until completion, and on timeout/kill the partial e.stdout is all that survives."
          },
          {
            "severity": "low",
            "area": "design-intent",
            "note": "The diff bundles unrelated spine metadata (task-124fde8a's full done record, board entries for task-10382dc9) alongside the cost-ledger change — inert queue state, but outside the 'diffs scoped to the current task' rule."
          }
        ]
      },
      {
        "at": "2026-07-06T09:49:27.052Z",
        "by": "adversary",
        "verdict": "pass",
        "findings": [
          {
            "severity": "med",
            "area": "regression",
            "note": "The executor is now the ONLY agent call site that does not apply withRunner: bin/chalk.mjs:685 and lib/run.mjs:25 pass protocol.executor.command raw into runExecutorCaptured, while plan (bin/chalk.mjs:429), review (lib/review.mjs:100), retro, discovery and feedback all wrap with withRunner(protocol.runner, cmd). If a repo sets protocol.runner (docs/CONFIG.md: prefix 'prepended to every gate command'), the executor invocation loses the prefix that every sibling stage gets, and no test exercises executor+runner. lib/cost.mjs's own comment ('a runner-prefixed command (fvm claude …) reads as not-claude') assumes callers pass the wrapped command — they don't. Verify on a runner-configured repo before relying on chalk run there."
          },
          {
            "severity": "low",
            "area": "regression",
            "note": "Only runExecutorCaptured got the 64MiB maxBuffer; review/retro/discovery/feedback keep execSync's 1MiB default while --output-format json now folds the agent's ENTIRE session output into one envelope line. A verbose reviewer whose envelope exceeds 1MiB hits ENOBUFS with truncated e.stdout, the unwrap fails, and the stage degrades to ms-only/parse-error. Pre-change output size was comparable so this is a narrow new edge, but the buffer fix was applied asymmetrically."
          },
          {
            "severity": "low",
            "area": "correctness",
            "note": "Criterion 2's 'without losing live terminal output' is satisfied by buffering a claude-shaped executor and re-emitting the inner result after exit (lib/cost.mjs:72-75) — content is preserved, liveness is not. Defensible because `claude -p` prints one final blob anyway, but a claude-shaped command that streams progress will appear frozen for up to the 10-minute timeout; the test pins re-emission, not liveness."
          },
          {
            "severity": "low",
            "area": "test-adequacy",
            "note": "Coverage is genuinely revert-detecting: every wired stage (review/retro/plan/discovery/feedback/executor) is pinned e2e through a fake `claude` on PATH that emits the envelope ONLY when the flag was injected, plus nonzero-exit stdout-first, banner-tolerant parse, >1MiB capture, pinned-format passthrough, non-claude ms-only, opportunistic self-emitted envelope, and legacy-only `chalk cost`. Residual gaps are minor: `turns` is never asserted in a live-harvested ledger record (only via parseEnvelope unit and the hand-written cost fixture), and non-claude executor live streaming (stdio-inherit) is untested. Ledger write failure (store.mjs logCost catch) is untested but fail-safe by construction."
          },
          {
            "severity": "low",
            "area": "design-intent",
            "note": "unwrapAgentOutput opportunistically unwraps ANY agent stdout that parses as a type:'result' envelope, including non-claude runners — intentional and tested, but a non-claude agent whose legitimate payload happens to be claude-envelope-shaped would be silently rewritten to its .result string. The type+result-string guard makes collision unlikely. Also, the diff again bundles unrelated queue metadata (task-10382dc9 spec, board rows) into .chalk/tasks.json — inert intake artifacts, slightly outside the 'diffs scoped to the current task' rule."
          }
        ]
      }
    ],
    "startedAt": "2026-07-06T09:26:40.228Z",
    "branch": "feat/99-feat-token-level-cost-ledger",
    "pr": {
      "number": 105,
      "url": "https://github.com/chalkagents/chalk-protocol/pull/105",
      "recorded": true,
      "lgtm": true
    },
    "doneAt": "2026-07-06T09:53:35.094Z"
  },
  {
    "id": "task-8189ac26",
    "title": "fix: spec-lock gate never checks locked tests are tracked in git — a pinned test can ship untracked and CI runs a vacuous green",
    "state": "done",
    "acceptanceCriteria": [
      {
        "text": "`chalk done` (and/or `chalk pr`) verifies every locked test path pinned in the task spec is tracked (`git ls-files`) in the task's worktree, and fails with an error naming the untracked path(s)"
      },
      {
        "text": "the error message suggests the fix (`git add <path>` / re-run `chalk commit`)"
      },
      {
        "text": "locked test: `chalk done` refuses when a pinned test file exists on disk but is untracked, and succeeds once the file is tracked"
      }
    ],
    "tests": [
      {
        "path": "test/spec-lock-tracking.test.mjs",
        "sha256": "0c91a5d9083f853e04f6d3e740987461e99580d0d9e70e30c4e99ac5cd71607f"
      }
    ],
    "heldOut": [],
    "after": [],
    "issue": {
      "number": 107,
      "url": "https://github.com/chalkagents/chalk-protocol/issues/107",
      "body": "Three reviews in this sweep (release --commit, config-drift gate, evidence-push) independently flagged the same hole: a locked test exists on disk with its sha256 pinned in `.chalk/tasks.json`, but the file is untracked — absent from `git diff --stat` — so a fresh checkout / CI (`node --test`) runs WITHOUT the contract tests guarding the criteria. `chalk done` verifies the sha256 against the working tree only; nothing checks the file is in git. The pipeline `commit` stage happens to sweep untracked files via `changedPaths` (`status --porcelain -uall`), but manual-order flows and anything done before/around the commit stage escape it, and the reviewer had to catch it every single time.\n\n- [ ] `chalk done` (and/or `chalk pr`) verifies every locked test path pinned in the task spec is tracked (`git ls-files`) in the task's worktree, and fails with an error naming the untracked path(s)\n- [ ] the error message suggests the fix (`git add <path>` / re-run `chalk commit`)\n- [ ] locked test: `chalk done` refuses when a pinned test file exists on disk but is untracked, and succeeds once the file is tracked\n\n_filed by `chalk retro` (self-healing)_"
    },
    "branchType": "fix",
    "labels": [
      "bug"
    ],
    "pipeline": {
      "stage": "cleaned",
      "at": "2026-07-06T12:21:06.239Z"
    },
    "createdAt": "2026-07-06T10:05:49.978Z",
    "reviews": [
      {
        "at": "2026-07-06T12:20:40.936Z",
        "by": "adversary",
        "verdict": "pass",
        "findings": [
          {
            "severity": "med",
            "area": "correctness",
            "note": "untrackedLockedTests() silently exempts any pinned path starting with '.chalk/' — criterion 1 says EVERY locked test path pinned in the spec must be verified. The exemption is justified in a comment (spine state lands out-of-band), but a task that pins an e2e spec under .chalk/ gets no tracking gate at all, and no test asserts this carve-out's behavior in either direction."
          },
          {
            "severity": "low",
            "area": "correctness",
            "note": "In `chalk pr`, the already-open-PR early return (t.pr set) exits BEFORE the tracking gate, so a test that becomes untracked after PR open (e.g. `git rm --cached`) is never re-checked on subsequent `chalk pr` invocations; `chalk done` still catches it, so the criterion's 'and/or' is technically satisfied."
          },
          {
            "severity": "low",
            "area": "correctness",
            "note": "Path matching is exact string equality against `git ls-files -z` output — a pinned path written as './contract.test.mjs', with backslashes on Windows, or differing only in case on a case-insensitive filesystem would be reported as untracked (false block). No normalization, no test for these forms."
          },
          {
            "severity": "low",
            "area": "test-adequacy",
            "note": "Core contract is genuinely covered and would fail on revert: locked test/spec-lock-tracking.test.mjs asserts done refuses (naming the path, saying why, suggesting `git add`), does not flip state to done, succeeds after `git add`, that pr blocks BEFORE pushing, and that non-git trees are exempt. Remaining untested surface is only the .chalk/ exemption and multi-path error rendering (tests use a single pinned file)."
          },
          {
            "severity": "low",
            "area": "regression",
            "note": "Gate runs in `done` only after verify passes and returns [] for empty pins or non-git trees, so existing done/pr flows without pinned tests are unaffected; existing pipeline/testgate suites (missingRequiredTest) are untouched."
          }
        ]
      }
    ],
    "startedAt": "2026-07-06T12:08:49.593Z",
    "branch": "fix/107-fix-spec-lock-gate-never",
    "pr": {
      "number": 115,
      "url": "https://github.com/chalkagents/chalk-protocol/pull/115",
      "recorded": true,
      "lgtm": true
    },
    "doneAt": "2026-07-06T12:21:06.239Z"
  },
  {
    "id": "task-056bac99",
    "title": "ci: add a windows-latest lane and fix what breaks",
    "state": "todo",
    "acceptanceCriteria": [],
    "tests": [],
    "heldOut": [],
    "after": [],
    "issue": {
      "number": 84,
      "url": "https://github.com/chalkagents/chalk-protocol/issues/84",
      "body": "**From the harness review (finding 10).** The suite is hermetic `node --test` with zero deps, but Windows is entirely unexercised: `command -v` in doctor's onPath, shell quoting in git/stash commands, and path edges are the likely breakage points. The demo and all gates are pure Node otherwise.\n\n**Sketch:** add `windows-latest` to the test matrix in `.github/workflows/test.yml`, triage failures, fix or conditionally skip with an issue link. Until then README should state Windows is best-effort."
    },
    "branchType": "feat",
    "labels": [
      "help wanted"
    ],
    "pipeline": {
      "stage": "selected",
      "at": "2026-07-06T10:05:49.982Z"
    },
    "createdAt": "2026-07-06T10:05:49.982Z",
    "reviews": []
  },
  {
    "id": "task-b9b5128a",
    "title": "feat: configurable e2e spec pattern (not just *.test.yaml)",
    "state": "todo",
    "acceptanceCriteria": [],
    "tests": [],
    "heldOut": [],
    "after": [],
    "issue": {
      "number": 83,
      "url": "https://github.com/chalkagents/chalk-protocol/issues/83",
      "body": "**From the harness review (finding 10).** `isSpec()` hardcodes the `.test.yaml` suffix; teams with a different browser-spec convention silently get no e2e gate on their locked specs.\n\n**Sketch:** `protocol.e2e.specPattern` (glob or suffix list, default today's behavior); doctor's existing 'locks a spec but e2e is off' warning should honor it."
    },
    "branchType": "feat",
    "labels": [
      "good first issue"
    ],
    "pipeline": {
      "stage": "selected",
      "at": "2026-07-06T10:05:49.983Z"
    },
    "createdAt": "2026-07-06T10:05:49.983Z",
    "reviews": []
  },
  {
    "id": "task-4bcc8640",
    "title": "feat: held-out set outside the repo root (manual-mode blindness)",
    "state": "todo",
    "acceptanceCriteria": [],
    "tests": [],
    "heldOut": [],
    "after": [],
    "issue": {
      "number": 82,
      "url": "https://github.com/chalkagents/chalk-protocol/issues/82",
      "body": "**From the harness review (finding 1, manual mode).** In worktree mode the gitignored `.chalk/held-out/` never reaches the agent's sandbox — but in MANUAL mode the agent works in the primary checkout and can simply read it. The contract forbids it; nothing enforces it.\n\n**Sketch:** `regression.dir` already exists — support an absolute/outside-the-repo path (e.g. `~/.chalk-held-out/<project>`) end-to-end (audit, doctor checks, guard authoring), and have `chalk doctor` recommend it when no worktree isolation is configured."
    },
    "branchType": "feat",
    "labels": [
      "enhancement",
      "good first issue"
    ],
    "pipeline": {
      "stage": "selected",
      "at": "2026-07-06T10:05:49.984Z"
    },
    "createdAt": "2026-07-06T10:05:49.984Z",
    "reviews": []
  },
  {
    "id": "task-2c8e0e36",
    "title": "feat: context budget — cap buildContext size and prune injected lessons",
    "state": "todo",
    "acceptanceCriteria": [],
    "tests": [],
    "heldOut": [],
    "after": [],
    "issue": {
      "number": 81,
      "url": "https://github.com/chalkagents/chalk-protocol/issues/81",
      "body": "**From the harness review (finding 6).** `buildContext` injects spec + criteria + lessons + prior reviews + handoff with no size cap; `lessons.md` grows forever. Large/old projects will degrade the executor silently (or hit stdin limits).\n\n**Sketch:** a byte budget (config, generous default) with priority order (criteria > locked tests > handoff > recent lessons > older material elided with a note); `chalk archive` already compacts tasks/events — extend the idea to lessons."
    },
    "branchType": "feat",
    "labels": [
      "enhancement"
    ],
    "pipeline": {
      "stage": "selected",
      "at": "2026-07-06T10:05:49.986Z"
    },
    "createdAt": "2026-07-06T10:05:49.986Z",
    "reviews": []
  },
  {
    "id": "task-86b9cfe4",
    "title": "feat: opt-in all-locks integrity — done tasks' locked tests stay protected",
    "state": "todo",
    "acceptanceCriteria": [],
    "tests": [],
    "heldOut": [],
    "after": [],
    "issue": {
      "number": 80,
      "url": "https://github.com/chalkagents/chalk-protocol/issues/80",
      "body": "**From the harness review (finding 5).** `verify` sweeps locked-test integrity only for in-progress tasks, so lock protection expires at `done`: a later task can weaken an earlier task's locked test to keep its own verify green (the ImpossibleBench cheat, one task removed). Held-out (P7) is the backstop but is opt-in.\n\n**Sketch:** `protocol.integrity: \"all-locks\"` → verify hashes every done task's locked tests too; `chalk amend-spec` stays the sanctioned change path. Tradeoff to document: legitimate test evolution requires amend-spec on old tasks."
    },
    "branchType": "feat",
    "labels": [
      "enhancement"
    ],
    "pipeline": {
      "stage": "selected",
      "at": "2026-07-06T10:05:49.986Z"
    },
    "createdAt": "2026-07-06T10:05:49.986Z",
    "reviews": []
  },
  {
    "id": "task-30ee8dd7",
    "title": "feat: tamper-evident spine — warn when tasks.json/chalk.json changed outside chalk",
    "state": "todo",
    "acceptanceCriteria": [],
    "tests": [],
    "heldOut": [],
    "after": [],
    "issue": {
      "number": 79,
      "url": "https://github.com/chalkagents/chalk-protocol/issues/79",
      "body": "**From the harness review (finding 1).** Only locked tests and held-out files are hashed today. In manual mode nothing detects an agent hand-editing `.chalk/tasks.json` (mark-done-by-hand) or weakening `chalk.json` verify commands — enforcement is process discipline.\n\n**Sketch:** record spine-file hashes in `.chalk/local/` after every CLI write; on the next invocation, if the on-disk hash differs from the recorded one, print a loud ⚠ (and log an event). Not a lock — tamper-EVIDENCE. The threat model is a sloppy/pressured agent, not a malicious human."
    },
    "branchType": "feat",
    "labels": [
      "enhancement",
      "help wanted"
    ],
    "pipeline": {
      "stage": "selected",
      "at": "2026-07-06T10:05:49.987Z"
    },
    "createdAt": "2026-07-06T10:05:49.987Z",
    "reviews": []
  },
  {
    "id": "task-4d462e08",
    "title": "feat: chalk stats — gate-efficacy report from the event log",
    "state": "done",
    "acceptanceCriteria": [
      {
        "text": "chalk stats reads tasks and events from the live spine AND .chalk/archive/, and reports review-gate efficacy: reviewed-task count, tasks blocked at least once before passing (catches), total block verdicts, and findings broken down by severity and by area"
      },
      {
        "text": "It reports churn: executor attempts and handoffs (totals and the worst offenders), and counts verify-RED blocks from the event log"
      },
      {
        "text": "It reports the gate-vs-bypass fraction over done tasks: the share that passed adversarial review vs done via review override (Overrode review gate decisions) or with no review, and the share landed through the pipeline (pipeline stage merged/cleaned) vs hand-landed"
      },
      {
        "text": "chalk stats --json emits the same report as machine-readable JSON; --since <date> restricts to tasks done and events emitted at/after that date; the command is pure read (never writes the spine), zero new dependencies"
      },
      {
        "text": "On a fresh spine with no tasks or events it prints a friendly empty-state message and exits 0 instead of throwing"
      }
    ],
    "tests": [
      {
        "path": "test/stats.test.mjs",
        "sha256": "dd6bc0a6bcfdc2ea3b1bc4ef13204c2d4ab62b52870c684ef92a81dfcefa72e9"
      }
    ],
    "heldOut": [],
    "after": [],
    "issue": {
      "number": 78,
      "url": "https://github.com/chalkagents/chalk-protocol/issues/78",
      "body": "**From the harness review (finding 8: no self-measurement).** Chalk never reports what its gates caught, so it can't prove its own value — to users or to us.\n\n**Sketch:** read `.chalk/updates.jsonl` (+ archive) and report per task/wave: verify RED→GREEN cycles, review blocks + findings by area, break-it/mutation catches, INCONCLUSIVE probe warnings, churn (attempts/handoffs). Zero deps, pure read. `chalk stats [--since] [--json]`.\n\nThis repo's own oss-launch wave is a ready-made dataset: 5 of 9 gated tasks were BLOCKED by the adversarial reviewer before passing."
    },
    "branchType": "feat",
    "labels": [
      "enhancement",
      "help wanted"
    ],
    "pipeline": {
      "stage": "cleaned",
      "at": "2026-07-06T10:28:24.327Z"
    },
    "createdAt": "2026-07-06T10:05:49.988Z",
    "reviews": [
      {
        "at": "2026-07-06T10:18:37.157Z",
        "by": "adversary",
        "verdict": "block",
        "findings": [
          {
            "severity": "high",
            "area": "test-adequacy",
            "note": "Criterion 2 explicitly requires 'worst offenders', but the locked test (test/stats.test.mjs) never asserts churn.worst — deleting worstChurn from lib/stats.mjs (or hardcoding []) leaves the entire suite green. A criterion-named behavior shipped with zero test coverage is a vacuous pass."
          },
          {
            "severity": "med",
            "area": "correctness",
            "note": "Criterion 2 reads 'executor attempts and handoffs (totals and the worst offenders)', but worst offenders are computed only for attempts; handoffs is a bare total (lib/stats.mjs:75). Handoff events carry taskId ('Handoff written: …', lib/handoff.mjs:86), so a per-task handoff breakdown is feasible and is simply missing."
          },
          {
            "severity": "med",
            "area": "correctness",
            "note": "--since validation is a type check only (bin/chalk.mjs:898). An unparseable date (`--since garbage`) makes Date.parse return NaN, and `if (cut)` in computeStats (lib/stats.mjs:43) is falsy for NaN — the filter is silently skipped and the full-history report is printed labeled 'since garbage'. Same falsy hole for epoch dates (cut===0). No test covers an invalid --since."
          },
          {
            "severity": "med",
            "area": "regression",
            "note": "verifyRedBlocks and overridden rely on brittle free-text matching against event wording produced elsewhere ('verify RED after executor'/'without a green verify' in lib/run.mjs:44-45; 'Decision: Overrode review gate for \"…\"' via lib/store.mjs:188 + bin/chalk.mjs:1354). Any future rewording silently zeroes these stats; no test couples the real emitters to the stats parser — the locked test hand-crafts the strings, so drift would go undetected."
          },
          {
            "severity": "low",
            "area": "correctness",
            "note": "Override attribution matches by task TITLE, not id (lib/stats.mjs:84-88): two done tasks sharing a title would both count as overridden, and a task overridden then later re-reviewed to pass is classified gated (overridden requires !gated), skewing the bypass fraction. Untested either way."
          },
          {
            "severity": "low",
            "area": "test-adequacy",
            "note": "No positive-path assertions for the 'no review' bypass bucket (unreviewed is only asserted to be 0), nor for landing.handLanded, review.passes, or audit.red — several reported fields could regress without failing the suite."
          },
          {
            "severity": "low",
            "area": "design-intent",
            "note": "The submitted diff contains ONLY .chalk spine files (boards/tasks/updates); lib/stats.mjs, the bin/chalk.mjs wiring, and the locked test test/stats.test.mjs exist in the working tree but are outside the presented diff. Confirm test/stats.test.mjs and lib/stats.mjs are actually tracked/committed on the PR #109 branch — an untracked pinned test is exactly the vacuous-green hole this repo just filed as issue #107 (task-8189ac26)."
          }
        ]
      },
      {
        "at": "2026-07-06T10:27:51.278Z",
        "by": "adversary",
        "verdict": "pass",
        "findings": [
          {
            "severity": "low",
            "area": "correctness",
            "note": "pipelineLanded (lib/stats.mjs:104) requires t.pr?.number IN ADDITION to pipeline.stage merged/cleaned, while criterion 3 says only 'pipeline stage merged/cleaned'. A task that reached the cleaned stage but lost its pr record would be miscounted as hand-landed. Both fixture tasks carry a pr, so the extra condition is untested either way."
          },
          {
            "severity": "low",
            "area": "correctness",
            "note": "Under --since, events missing an `at` field hit Date.parse(0) → Date.parse('0'), which V8 parses as 2000-01-01 rather than epoch/NaN, so undated events are silently included in windows after 2000. All real emitters set `at`, so this is a defensive-path quirk only."
          },
          {
            "severity": "low",
            "area": "test-adequacy",
            "note": "The prior block's --since falsy hole is properly fixed (`cut !== null`, lib/stats.mjs:49) and garbage input is now rejected at both the CLI (bin/chalk.mjs:899) and computeStats (lib/stats.mjs:46) with a locked test — but the epoch boundary itself (--since 1970-01-01) and the `--json` empty-spine path (prints an all-zero report instead of the friendly message; criterion 5 arguably only covers the human report) are not pinned."
          },
          {
            "severity": "low",
            "area": "regression",
            "note": "Refactoring run/handoff/done/audit emitters onto lib/markers.mjs preserves the exact legacy strings ('Handoff written:', 'Blocked: … (needs …)', 'Decision: Overrode review gate for \"…\"', 'Audit green/red'), and the two end-to-end tests (chalk run verify-RED+handoff, chalk done --force-review) now couple emitters to the parser as the amend-spec demanded — drift risk from the earlier review is addressed; residual risk is limited to third-party tooling that greps the old literals, which are unchanged."
          }
        ]
      }
    ],
    "branch": "feat/78-feat-chalk-stats-gate-efficacy",
    "startedAt": "2026-07-06T10:12:08.370Z",
    "pr": {
      "number": 109,
      "url": "https://github.com/chalkagents/chalk-protocol/pull/109",
      "recorded": true,
      "lgtm": true
    },
    "doneAt": "2026-07-06T10:28:24.326Z"
  },
  {
    "id": "task-11ea6ea9",
    "title": "feat: give reviewer-induced auto-blocks a distinct `--needs` category instead of `human-input`",
    "state": "specd",
    "acceptanceCriteria": [
      {
        "text": "Add a dedicated `--needs` value (e.g. `review`) to the taxonomy in CLAUDE.md and the block command validation."
      },
      {
        "text": "Emit reviewer-induced blocks in lib/pipeline.mjs with the new category instead of `human-input`."
      },
      {
        "text": "`chalk status` / next surface the review block distinctly so it is not confused with a real human dependency."
      },
      {
        "text": "Add a test asserting a reviewer block sets the review-specific `needs` while a genuine stage failure stays `human-input`."
      }
    ],
    "tests": [
      {
        "path": "test/review-block-needs.test.mjs",
        "sha256": "a49a2f3b08b1e8a1cd65899e47ed1e57ba169f7eb2cc1b0c5bb34c8058e38965"
      }
    ],
    "heldOut": [],
    "after": [],
    "issue": {
      "number": 46,
      "url": "https://github.com/chalkagents/chalk-protocol/issues/46",
      "body": "In lib/pipeline.mjs a failed review (line 69) and a genuinely human-blocking stage failure (line 76) both call `block ... --needs human-input`. Only the free-text reason distinguishes them, so any downstream tooling/triage that routes by `block.needs` treats a reviewer-induced block identically to a credentials/decision/human-input block. A reviewer block is structurally different: it was produced by chalk's own adversarial reviewer, often after a retry, and is the agent's responsibility to address — not a pending human dependency.\n\n- [ ] Add a dedicated `--needs` value (e.g. `review`) to the taxonomy in CLAUDE.md and the block command validation.\n- [ ] Emit reviewer-induced blocks in lib/pipeline.mjs with the new category instead of `human-input`.\n- [ ] `chalk status` / next surface the review block distinctly so it is not confused with a real human dependency.\n- [ ] Add a test asserting a reviewer block sets the review-specific `needs` while a genuine stage failure stays `human-input`.\n\n_filed by `chalk retro` (self-healing)_"
    },
    "branchType": "feat",
    "labels": [
      "enhancement"
    ],
    "pipeline": {
      "stage": "committed",
      "at": "2026-07-06T12:28:49.623Z"
    },
    "createdAt": "2026-07-06T10:05:49.989Z",
    "reviews": []
  }
]
